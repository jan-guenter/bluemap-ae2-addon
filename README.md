# BlueMap AE2 Add-on

A BlueMap 5.22 add-on for statically rendering Applied Energistics 2 blocks
whose geometry is normally constructed by Minecraft client code.

## Current status

**M4/M5 is the complete owner-accepted All the Mons 1.2.0 local checkpoint.
Its reproducible artifacts, schema-11 evidence, isolated technical lifecycle,
and bounded BlueMap visual review passed. Tag `v0.1.0-alpha.1` was published as
an immutable GitHub prerelease with attestations and Maven coordinate
`io.github.jan-guenter:bluemap-ae2-addon:0.1.0-alpha.1` on 2026-08-12.** The exact
target is All the Mons `1.2.0` at pack commit
`c7bb230f21d14d26859d0b92548f089b3a493ad9`, Minecraft `1.21.1`, NeoForge
`21.1.248`, Java `21`, AE2 `19.2.17`, AppliedFlux
`1.21-2.1.5-neoforge`, ME Requester `1.21.1-1.4.3`, Expanded AE `2.1.1`,
MEGA Cells `4.11.0`, Advanced AE `1.6.12-1.21.1`, Athena `4.0.6`, ExtendedAE
`1.21-2.2.35-neoforge`, and BlueMap backport
`5.22-agent.backport-5.22-mc1.21.1-2` at commit
`9be321df995a1103808621d529eb72773e719d4d`. Its canonical 6,467,235-byte
NeoForge JAR has SHA-256
`749f7647fa29764cea113114a7ab3259271bab3da22720989f2bd9fd1f3ba150`.

The owner accepted the exact `0.1.0-alpha.2` Applied Mekanistics review on
2026-08-19 and separately authorized publication. It was published as
immutable prerelease `v0.1.0-alpha.2` on 2026-08-20. This successor adds exact
Applied Mekanistics `1.6.3` support against exact Mekanism `10.7.19` through the
independent `appmek-drive-cells` route: ten chemical-cell IDs map to five exact
chassis models in native `ae2:drive` only. Its expanded verifier locks ten
artifacts and twelve resource manifests with 381 rows. Schema-12 also carries
bounded parent-renderer seam regressions for AE2 storage buses against two
Mekanism blocks and a Mekanism pressurized tube against the full-block AE2
interface. The seven-anchor runtime gate passed with exact triangle counts
`250/106/106/106/74/74/18`, zero fixture failures, and a naturally connected
tube/interface seam. No AppMek or Mekanism class, asset, source, or binary is
bundled.

The owner accepted the `0.1.0-alpha.3` registration-hook staging on
2026-08-25. Exact Ars Energistique `2.1.1-beta` loaded in All the Mons `1.2.0`
with AE2 `19.2.17` and Ars Nouveau `5.13.0`. The 25 accepted cells included
three externally registered part types across all six faces and five source
cells in one native Drive. The public BlueMap view was sanity-checked by the
agent and accepted by the owner. This evidence is bounded to disposable
staging and is not a production claim.

The published alpha.1 Java runtime audit and exact eight-artifact,
eleven-manifest/375-row,
67-cell-catalog M45 verifier passed. Two byte-identical builds each ran 562
Java tests (560 passed and two intentional exporter tests were skipped) and
180 pre-oracle Python tests. The final frozen-oracle CPython 3.13.14 suite
passed 192/192 tests in 945.343 seconds together with the generator/checksum
closure.

| Accepted runtime/evidence artifact | Size | SHA-256 |
| --- | ---: | --- |
| Production JAR | 1,207,683 bytes | `6fed7a625b02229213a047788995944f14e7e7fcabe0e0ddc6d9b5e994146e9f` |
| Sources JAR | 532,979 bytes | `4a45c60f8512630c6bd9735e26018d019ebe99d58f2c87fa2f3c46e101b624d8` |
| POM | 1,637 bytes | `967132ef80201099cfb1a798f03ff1ac37e0ac84a551694d7276ac20c7ccc136` |
| Gradle module metadata | 2,861 bytes | `3f0ba24c34ef535c99cbd6dabcd7d6bb0f784ca6ffd032f06faaf9a9b5d7b0b8` |
| Schema-11 gallery ZIP | 94,537 bytes | `c67b4f794092f6e994349a8ee9320c052e2efc87f04e8813faf158c3455fe33b` |
| Schema-11 `cases.json` | 6,017,554 bytes | `914dab6931077521959cf59260a1ffb0cdbe105385f43880763b289f8117ec55` |
| M4/M5 main oracle | 221,769 bytes | `c2ce69bed949306551ca4ff6cdebf7fac88f0f2f2fa7ab294d3312f363e1b448` |
| M4/M5 legacy-upgrade oracle | 2,336 bytes | `2319ecf576ba07b123078c720d941990fac939033d375e5853f51bf98348c3c7` |
| Enabled map archive | 20,821,895 bytes | `44422aa71c2f450951d8433e25e01de7a0b00dbd0d9c4fa4ff74ca98e649a2df` |

The separately rebuilt release-version outputs are:

| Published artifact | Size | SHA-256 |
| --- | ---: | --- |
| Production JAR | 1,207,650 bytes | `8372b4f043f3091f6aa1fed6a76006e5e04cbf462647472beb392c9789da9e03` |
| Sources JAR | 532,770 bytes | `b38c7c671730d88849bdd86cce30ce911a94b023b7e1d487baa1977dc9663bea` |
| POM | 1,619 bytes | `129f5e572e7bc77618a5739ba2999bd3559e3644608b8aaa7b9f604b00c9d528` |
| Gradle module metadata | 2,794 bytes | `9277aab0b8e66703140a020747f7a60174bf9453b01de52e08ff7b068a848d9a` |

Download the immutable prerelease from
[GitHub Releases](https://github.com/jan-guenter/bluemap-ae2-addon/releases/tag/v0.1.0-alpha.1).
The Maven coordinate is
`io.github.jan-guenter:bluemap-ae2-addon:0.1.0-alpha.1`.

S1 native cable-bus structural completeness remains the accepted predecessor:
its All the Mons 1.2.0 isolated lifecycle and owner BlueMap visual review passed
on 2026-08-11. The M4/M5 candidate aliases remain on `data-atm120` beside
hash-exact accepted aliases `ae2-addon-m5-accepted.jar`,
`ae2-m5-gallery-accepted.zip`, and
`ae2-m5-enabled-accepted-map-2026-08-11.tar.gz`.

The exact M0 runtime artifact
`84a3b972d86a49a723e56a820ff3b59039654e2108d8ba965493d2302a5b1e41`
passed the bounded build, deterministic-render and enabled/stock/re-enabled
technical gates and was human accepted in both the modded client and BlueMap
on 2026-08-04. That acceptance covers only its eight-logical-case,
17-anchor fluix-glass M0 gallery.

The exact 161,930-byte M1 SNAPSHOT JAR with SHA-256
`e02beee7fdafeba9c3ef0ea42deda0a7709cc70df23d4778cfb7a72b1fdaf2e1`
passed its reproducible build, enabled/stock/re-enabled lifecycle,
deterministic rerender, two-thread smoke and opt-in dense-fixture gates on
2026-08-04. It was human visually accepted on 2026-08-07. That decision is
bounded to this exact M1 JAR and did not pre-establish the independently
accepted M2 result below.

The accepted M1 behavior targets all 85 AE2 19.2.17 center cables: 17 colors
across glass, covered, smart, covered-dense and smart-dense families. It models
color compatibility, mixed-family effective connection sizes, family-specific
cores and arms, exact straight-cable simplification, and the zero-channel smart
overlays required by the static policy.

The accepted M2 milestone adds one static reporting face-part ID,
`ae2:terminal`, across all six faces and persisted byte spins `0..3`. It
supports multiple terminals and orientations on a non-dense accepted center.
The only facade case is one canonical `{Name:"minecraft:stone"}` state with
no `Properties`, on the same face as exactly one terminal. Dense-plus-part,
standalone part, unsupported part/spin, property-bearing or non-stone facade,
multiple facade, mismatched face, extra-part facade and ambiguous endpoint
conditions all use the original AE2 resource for the complete block.

The M2 input gate covers 85 center IDs, one face-part ID, 148 operator-installed
AE2 textures and 170 resources. Its schema-3 gallery contains 62 cases and 290
anchors: 278 custom anchors with 8,576 custom triangles and 149 selected
runtime materials, 11 zero-triangle stock-fallback anchors and one vanilla
stone control. The exact 203,599-byte local M2 SNAPSHOT JAR has SHA-256
`fc11af62359746990a2b35470c1da66e606b13a36be33a5b854d343eebb108d2`.
Two clean builds reproduced it, and all 120 Java and 38 Python tests passed.

The first enabled cold/warm staging captures validated all 290 anchors, with
8,586 selected triangles in total and no triangle leakage into the 11 atomic
fallback anchors. Their reports and 44 relevant non-live/non-`rstate` web
files were byte-identical. A physically add-on-absent stock restart selected
only the 10-triangle stone control; the other 289 anchors remained empty and
its cold/warm reports and 44 comparable web files were also byte-identical.
The fixture verifier stayed at zero failures after delayed verification,
chunk unload/reload and a full JVM restart, with the critical supported and
unsupported NBT states preserved. Observed pods had zero restarts and both
configured render threads accumulated CPU time. Restoring the exact M2 JAR and
`ae2_staging` configuration activated the profile, retained all four critical
NBT states with zero verifier failures without calling `build`, and reproduced
the initial enabled report and all 44 relevant web files byte-for-byte. The M2
technical/lifecycle candidate has therefore passed; human visual acceptance
of this exact JAR and bounded M2 gallery was recorded on 2026-08-07. That
acceptance does not extend to another artifact, AE2 version or unsupported
topology.

M3a adds only the native `ae2:drive`. It supports all 24 persisted
`facing`/`spin` states, ten inventory slots and a closed catalog of 23 exact
AE2 item IDs mapped to 12 occupied chassis models. An empty Drive emits the
90-triangle operator-installed base; each occupied slot adds six chassis and
ten black offline-unknown LED triangles, for `90 + 16N` triangles. Item
components do not change that static representation. Unknown extension cells,
including the gallery's MEGA Cells control, and malformed or unsupported state
fall back atomically to the original Drive resource.

The exact M3a profile covers 158 AE2 textures and 196 required resources. Its
profile SHA-256 is
`2c27976a718834dbc97b3eb7cac6543c4ad2a966737c7bccbadb2b1c39c837e8`;
the full resource-manifest SHA-256 is
`408297def444f1392b7b87fdc4b8520099513b4c57c63a4176b808ce61b4e1be`.
Two clean builds reproduced the exact 259,005-byte production JAR with
SHA-256
`55a11805373aebfde821e5009723ec7d672fb290127dbc60131ffa344c99518a`
and 136,798-byte sources JAR with SHA-256
`40a06a77768f085d4680cb5eae435b35b6115482f231874cd4f13fac73b6edea`.
All 173 Java and 45 Python tests passed. The exact 26,633-byte gallery ZIP has
SHA-256
`91057d94890bc3bd063fb7c4bf951f9a70b62a00ee01a9b00deb0fd8f674bb2b`.

The schema-4 gallery has 76 cases and 323 anchors. Its enabled cold/warm
captures selected 310 custom anchors, 12,432 custom triangles, all 159
expected runtime materials, 12 zero-triangle atomic fallbacks and the
10-triangle stone control. Their 939,040-byte reports and all 44 comparable
non-live/non-`rstate` files were byte-identical. With the add-on physically
absent, the stock cold/warm captures selected only the stone control; their
322,637-byte reports and the same 44-file set were also byte-identical. The
fixture persisted without rebuilding across delayed verification, actual
chunk unload/reload and complete JVM restarts. Accepted capture phases had
zero pod restarts and no unexpected/add-on restart occurred; one restart
attempt was induced by operator timing while transitioning between phases and
is not concealed as an add-on result. The final restored pod was Ready with
restart count zero, verified the fixture and four critical NBT probes without
invoking `build`, and activated both exact routes. Its report and 44-file
manifest were byte-identical to the enabled cold/warm captures. A deterministic
44-file map archive was reproduced twice at 20,320,750 bytes with SHA-256
`18cd08c9f0de6bc132bbf9f5cbf1d692d475864ca9aa818eed6166d892a94dce`.
The complete M3a technical lifecycle has therefore passed; see
[staging](docs/STAGING.md) for the exact identities and evidence boundary.
The owner visually accepted this exact M3a JAR and bounded gallery on
2026-08-07. That acceptance closes M3a only and does not transfer to M3b.

M3b adds only the exact pack-pinned `extendedae:ex_drive` through a separate
fail-closed profile and renderer route. It supports all 24 `facing`/`spin`
states and twenty persisted slots: `0..9` on the front and `10..19` on the
rear, where the rear uses the opposite facing with the same spin. Its closed
catalog is the accepted 23 native AE2 IDs plus ExtendedAE's three built-in
infinity-water, infinity-cobblestone and void cells, mapped to 15 occupied
models. An empty Extended Drive emits 116 triangles; each occupied slot adds
six chassis and ten black fullbright offline-unknown LED triangles, for
`116 + 16N`. Dynamic KubeJS cells, MEGA Cells, unknown items, invalid counts
and malformed state use atomic whole-block fallback.

The historical accepted M3b ExtendedAE input is a 5,573,972-byte JAR with SHA-256
`6652ed1ea4b71f585d48c05a195a77594a7a2bd1ecea0fc805db2122aafad734`.
The disjoint profile binds 15 resources and eight runtime textures and depends
on the already accepted 26-resource AE2 Drive partition. Two clean builds
reproduced these local M3b artifacts:

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| Production JAR | 323,416 bytes | `f02123cb602bb7b6466d1529c5518e45862f53f413ce9a75ecc067d1a30607d1` |
| Sources JAR | 166,516 bytes | `8574cf425d5e76c527e02eeeda1f3805845cfe9e6cad6f6fa2f6457aa2157909` |
| POM | 1,637 bytes | `967132ef80201099cfb1a798f03ff1ac37e0ac84a551694d7276ac20c7ccc136` |
| Gradle module metadata | 2,859 bytes | `5a739c3cd624ad3c2313bc49c77bfe66d54df6851a321f46ce6f1d94084e5386` |
| Gallery ZIP | 37,614 bytes | `69bdb99d9c8f6838c3b8d5847c32702761cfa77b263ee95384ca24357c84cf92` |

All 217 Java and 56 Python tests passed. The 1,143,610-byte schema-5
`cases.json` has SHA-256
`5a1297668b6922b03ae3f2ab089b643aa9521060e61607bbbae353a80d8494fc`.
Its 92 cases and 359 anchors contain 342 custom anchors, 17,488 custom
triangles, 167 selected resources, 16 zero-triangle atomic fallbacks and one
stone control.

The enabled cold, warm and restored captures produced byte-identical
1,335,214-byte reports with SHA-256
`e5e466d9f207beba2032775b66acf091c5c3bf647b40bc811b62a714dae6dd8e`
and byte-identical 44-file manifests with SHA-256
`1b1db461fc8e76d4e988bd60c403af8601c51cf4d6fac0bfb4e748df5c4c60e9`.
The independent extension-disabled cold/warm pair retained the complete
accepted M3a result while all 36 M3b anchors stayed empty; its 1,006,824-byte
report SHA-256 is
`2947419d53cf7e2e2f8a4a3cacecc2515eb6aec2382f328e48fb596a98c9d754`.
The physically add-on-absent stock pair selected only the ten-triangle stone
control; its 365,850-byte report SHA-256 is
`630c96be1406504267e27871e6113b0d98d23da522c9358c31f2f61efaff6f1a`.
The fixture was built once, verified with zero failures across unload/reload
and full-JVM transitions, and retained six critical NBT probes. All accepted
capture pods had zero restarts. One pre-runtime init sequencing attempt ended
before Minecraft started and caused no add-on restart. A deterministic
20,351,418-byte 44-file map archive was reproduced twice with SHA-256
`c73844990847148d9cd3d315832085e49776e9253c5c8eca6f0b7659d73c4285`;
both configured render threads accumulated 420 and 418 scheduler jiffies in
the bounded smoke. On 2026-08-07 the owner visually accepted the exact
323,416-byte JAR, the exact 37,614-byte gallery ZIP and this exact map archive.
That acceptance is bounded to their identities and M3b behavior.

At the M3c checkpoint, M3 remained active and incomplete. M3c has an
independent exact profile for
`ae2:quartz_glass` and `ae2:quartz_vibrant_glass`, a 19-texture/22-resource
operator-supplied closure, and a full exact-JAR verifier. The route manifest
SHA-256 is
`b51c708e7c4d26093c1b6f85b88d0be50572d3cfa76dbf802720f6ad79c7a7fa`;
the generated profile SHA-256 is
`548e5bc00ef07c6d6b93b346422b596882ec11ca03de006065fa45fecb991200`.
The existing M3a 196-resource main profile and manifest remain byte-identical.
The Java renderer, independent route activation, schema-6 11-case/47-anchor
gallery and fail-closed PRBM analyzer are implemented. Focused geometry,
resource, route, adapter, generator and analyzer gates pass, including Java
coverage of all 64 direct-neighbor masks. The gallery is deliberately
representative rather than a visual enumeration of all 64 masks.

Two clean builds reproduced the exact local M3c artifacts:

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| Production JAR | 375,558 bytes | `4c1b557ae4c79c738005b74e2f0c89ca4fbe503dd6ef0ba614fae34d8e449d47` |
| Sources JAR | 186,080 bytes | `6b011719264229629fb1011f4c6de566bf16b13603a6c2412b4ed4ced70a4036` |
| POM | 1,637 bytes | `967132ef80201099cfb1a798f03ff1ac37e0ac84a551694d7276ac20c7ccc136` |
| Gradle module metadata | 2,859 bytes | `5922575b3002d2cc48e1e5e2fa6a795b8b7f11349b1bb0d04f17db7c0b182876` |
| Gallery ZIP | 38,929 bytes | `0839009fe6a4f4785f864f33bc97fef28b8418f077d5d66a20efc3e8eeb4edab` |

All 247 Java and 71 Python tests passed. The 1,517,248-byte schema-6
`cases.json` has SHA-256
`2d4fbba58ea2c4d3ed741e93a8dd9857523cac9cda021ffd3111e6ac51aec602`.
Enabled cold, warm and restored captures produced byte-identical 1,582,336-byte
reports with SHA-256
`e81b8e6eed2047629a933a21e0e345c4880db2b12e264455fa84ef59b63d824f`
and byte-identical 44-file manifests with SHA-256
`3257a86e895956d7701f056ef46f3188fb4fca2f704b8c7e67641164666221f3`.
Extension-disabled retained all 47 M3c anchors while emptying M3b;
glass-disabled emptied all 47 M3c anchors while reproducing the accepted M3b
slice; physically add-on-absent stock retained only the stone control. Each
cold/warm pair was byte-identical. The restored enabled phase reproduced the
initial report and all 44 files without rebuilding.

The fixture was built once and stayed at zero verifier failures through
delayed verification, actual unload/reload, full JVM restarts, every route mode
and restoration. All six critical NBT probes remained unchanged. The exact
20,376,253-byte 44-file enabled map archive was reproduced twice with SHA-256
`3fb5fb174f23c0f2d8ce9f98e8c12feb8b12c444060e35b9d8e0036d8ec165e5`.
Every accepted capture pod had zero restarts and both configured render threads
accumulated CPU time. One operator Ctrl-C while detaching the console caused a
restart attempt during pre-lifecycle setup; it preceded the accepted captures
and was not an add-on restart. This passes the M3c technical lifecycle on
2026-08-08. The owner then visually accepted the exact 375,558-byte production
JAR, exact 38,929-byte gallery ZIP and exact 20,376,253-byte map archive that
day. Their SHA-256 identities are respectively
`4c1b557ae4c79c738005b74e2f0c89ca4fbe503dd6ef0ba614fae34d8e449d47`,
`0839009fe6a4f4785f864f33bc97fef28b8418f077d5d66a20efc3e8eeb4edab`
and
`3fb5fb174f23c0f2d8ce9f98e8c12feb8b12c444060e35b9d8e0036d8ec165e5`.
That bounded decision completes M3c; the accepted M3b artifact itself still
contains no M3c behavior.

The hash-exact accepted PVC aliases are `ae2-addon-m3c-accepted.jar`,
`ae2-m3c-gallery-accepted.zip` and
`ae2-m3c-enabled-accepted-map-2026-08-08.tar.gz`; the candidate aliases remain
available. These paths are staging conveniences rather than publication or a
durable provenance store.

M3d adds the independent `ae2-crafting` route for the eight native AE2
19.2.17 crafting-cube blocks. It custom-renders only exact `formed=true`
states; exact unformed states keep their stock models. The strict monitor
projection accepts persisted `paintedColor` byte ordinals `0..16`, retains
facing and spin for state validation, and deliberately omits the transient
displayed stack. Native crafting neighbors connect; missing or malformed
native state and the closed known compatible MEGA Cells/Expanded AE set use
atomic whole-block fallback. Other non-native neighbors remain disconnected.

Two clean builds reproduced the exact local accepted artifact:

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| Production JAR | 448,915 bytes | `ca057f025338150255ea916402c08bc8b614f9398a063e7433bbe468808c93ee` |
| Sources JAR | 213,004 bytes | `3e95cabf3e9dcf4ab5c8c2b6d6661ba6464a6cb3e6abd6d33fcfb904b5197c4f` |
| Gallery ZIP | 44,201 bytes | `4a18b45f2c03c8d1d3c49a731df2c2503745952faccf9ba06ec8f301909b81f3` |
| Schema-7 `cases.json` | 3,030,512 bytes | `c60d2afff5a1f92da4972963fcb926c38093f43bb6d7f550799f104349728a38` |
| Enabled map archive | 20,417,822 bytes | `672cdffaf5135f34c4b10c24638056540dcaadbb5fd2d78b3096897436d8a2c6` |

All 285 Java and 85 Python tests passed. The cumulative 112-case/492-anchor
gallery contains 474 custom anchors, 22,570 custom triangles, 201 selected
resources, 17 zero-triangle fallbacks and the separate ten-triangle stone
control. The one-build fixture retained zero verifier failures and two stable
checks through initial/delayed verification, actual unload/reload and full JVM
restarts. Every enabled, extension-disabled, glass-disabled,
crafting-disabled and stock cold/warm pair was byte-identical; restored enabled
cold and warm reproduced the initial enabled report and 44-file manifest.
Both render threads advanced by 313 and 520 scheduler jiffies, and capture pods
had zero restarts. One operator sequencing attempt failed during initialization
before Minecraft started and was not an add-on or capture restart.

On 2026-08-08 the owner visually accepted the exact production JAR, gallery
ZIP and enabled map archive above. The hash-exact accepted PVC aliases are
`ae2-addon-m3d-accepted.jar`, `ae2-m3d-gallery-accepted.zip` and
`ae2-m3d-enabled-accepted-map-2026-08-08.tar.gz`; the candidate aliases
`ae2-addon-m3d-candidate.jar`, `ae2-m3d-gallery-candidate.zip` and
`ae2-m3d-enabled-candidate-map-2026-08-08.tar.gz` remain retained. Neither
alias set is a publication store. M3d is complete as a bounded slice and was
the latest human-accepted result at that checkpoint; it is now the rollback
checkpoint before accepted M3e.

M3e adds the independently fail-closed `ae2-quantum-bridge` route for exact
`ae2:quantum_link` and `ae2:quantum_ring` states. It accepts only a fully
formed isolated 3x3x1 bridge in the XY, XZ or YZ plane, with the link at the
center and eight native rings split into four corners and four edges.
`formed` and `waterlogged` are the only authoritative saved render state. The
client-stream-only constructed-role, corner and powered bits are not decoded;
the route therefore emits the deterministic static-off-unknown projection and
excludes powered overlays and client particles. Invalid, incomplete or
ambiguous topology falls back atomically. No extension connector compatibility
is claimed.

The exact route profile has SHA-256
`21afa152e3f56d8bdde9f602748c0efbca52a2c55d5dd7a836adca267c65480e`.
Its sorted 13-resource, 3,798-byte closure has SHA-256
`717eed1ada75fb43c1324792c147cd8c2308d8c73ee82bf52d8de6bad4f74ed9`.
Eleven paths are route-only; the two transparent cable sprites are shared with
the frozen main profile. It audits six operator-installed source textures but
emits only the four non-powered textures. The exact 8,230,896-byte runtime JAR and exact
3,814,167-byte official sources JAR remain external evidence and are never
redistributed.

The supported topology boundary is deliberately closed: only the two exact
native block IDs participate, and an extra native quantum block in the six
surrounding face slabs makes the structure ambiguous and triggers atomic
original-resource fallback. Known nonnative blocks are disconnected. Client
particles, item rendering and live powered appearance are out of scope.
Geometry, material, UV, cutout, ambient occlusion and host world-light
semantics are covered by the exact local candidate. Ambient occlusion is
derived from neighboring world blocks and ordinary light remains the
BlueMap host value. Powered overlays and particles remain excluded; neither
ordinary chunk NBT nor the route claims to recover live quantum power.

Two clean builds reproduced the exact candidate artifacts:

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| Production JAR | 513,674 bytes | `98ff55eaba609fc894b01e0c4d922b47f1871c324945f88f7a34864cf48b124f` |
| Sources JAR | 234,963 bytes | `2bc749373eeb29bd30b9edb58006c7248da1cc09a6abdc7abb404b86a4045a1e` |
| POM | 1,637 bytes | `967132ef80201099cfb1a798f03ff1ac37e0ac84a551694d7276ac20c7ccc136` |
| Gradle module metadata | 2,859 bytes | `d1da10c42393c8a9cb79b77ad67a0b3d15140ef58fef593b20f25710fc8b0e02` |
| Schema-8 gallery ZIP | 45,009 bytes | `498bac2f82b78451eb24da416ded1d625e5785cc5d3e5910b4c34bfecc05c390` |
| Schema-8 `cases.json` | 3,123,572 bytes | `93963dd0bb60a276e1a17c6dd1f4eb916cd92bef4ef30a2e8bdc7a2bfa818b3e` |
| Enabled candidate map archive | 20,424,799 bytes | `9e145fffbe87205651ed7cc6b4cb706b7dcbe394ac26e7ce2eb1d6d55ea411a7` |

All 316 Java tests in 53 suites and all 98 Python tests passed. The schema-8
gallery has 115 cases and 519 anchors: 501 custom anchors own 23,758 custom
triangles and select 203 resources, while 17 atomic fallbacks own no
triangles. M3e adds three cases, 27 custom anchors and 1,188 triangles across
complete formed XZ, XY and YZ bridges. The accepted schema-7 M0-M3d view is
embedded unchanged at SHA-256
`c60d2afff5a1f92da4972963fcb926c38093f43bb6d7f550799f104349728a38`.

The cumulative fixture was built once and retained zero failures and two
stable checks through delayed verification, actual unload/reload, full JVM
restarts, five installed route modes, physically add-on-absent stock and
restored enabled. Every cold/warm report and exact 44-file manifest pair was
byte-identical, and restored enabled reproduced initial enabled exactly. Every
capture pod had zero restarts; both render threads advanced by 587 and 245
scheduler jiffies. The enabled archive above was reproduced twice with 44
entries. On 2026-08-08 the owner visually accepted this exact JAR, gallery and
map archive, completing M3e.

The exact candidate PVC aliases are `ae2-addon-m3e-candidate.jar`,
`ae2-m3e-gallery-candidate.zip` and
`ae2-m3e-enabled-candidate-map-2026-08-08.tar.gz`. They remain byte-identical
to the hash-exact accepted aliases `ae2-addon-m3e-accepted.jar`,
`ae2-m3e-gallery-accepted.zip` and
`ae2-m3e-enabled-accepted-map-2026-08-08.tar.gz`.

M3f is complete and human accepted under profile ID `ae2-m3-completion`. It
covers
persisted paint splotches, both closed Sky Stone chest variants, neutral crank
and structural Inscriber poses, and locally inferred static/offline spatial
pylons. Its exact local accepted identities are:

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| Production JAR | 623,591 bytes | `ca67c0fc433e43f8e0801ed8d2cccfe47aae317fbc329c099bc8cd741ec3b42b` |
| Sources JAR | 276,986 bytes | `2a3bb3713ff56731992d405a58fc6a137dcfc8fff43467de7196ad33c444795c` |
| POM | 1,637 bytes | `967132ef80201099cfb1a798f03ff1ac37e0ac84a551694d7276ac20c7ccc136` |
| Gradle module metadata | 2,859 bytes | `106df734036c9f72f0463a03e2a282d430653edab5e761b165b51fe99f937d7a` |
| M3-completion profile | 9,405 bytes | `281a335d3024ebbb97c6268e768826c467d6f7ea660989fd3dae204c6c03abf3` |
| Schema-9 gallery ZIP | 49,679 bytes | `21ceec072cc3263a41bdb81874e897d48d5a1ce5e1c7d3ac3c0de3063818ee6c` |
| Schema-9 `cases.json` | 3,314,082 bytes | `75e6ba2f40631a95f20cfa00d7ca952e521bc2c7a4eb155926334a223a945f3a` |
| Packaged support matrix | 9,684 bytes | `71ec7977c7990678b9f34b27b976a0a1381b85a292afd409556f8190cee88863` |
| Packaged provenance manifest | 84,990 bytes | `dd3bda10236288c5a8e745978e7f55467961bf3aab1a4cb8ab527e8b1eec6e50` |

Exact-source review corrected the pylon lane. It uses an uncached native-axis
scan capped at 256 pylons; every member of a fully observed invalid L- or
T-shaped component renders AE2's unformed `NONE` role with BASE plus DIM
materials. Missing, malformed or capped observation alone uses atomic
original-resource fallback.

The prior M3f candidate was withdrawn after visual review exposed invisible
L/T pylon members; it was never accepted. This corrected artifact supersedes
it and was visually accepted on 2026-08-09. That acceptance is bounded to the
exact JAR, gallery and map archive listed here.

All 365 Java tests and 116 Python tests passed. Enabled cold, warm and restored
reports are byte-identical at 3,783,797 bytes with SHA-256
`7022a33448dab364cb825a8d67359795560b6a8793b64544ccb0b4c1fda7484e`;
their exact 44-file manifests share SHA-256
`259af4eea91a32acc07d1572e8f3f42e6276b46999496d0380ec009c10970fd8`.
Independent M3-completion-disabled and physically add-on-absent stock
cold/warm pairs are also byte-identical; physical stock renders 38 M3f
anchors and leaves 40 empty. The 44-entry map archive was reproduced
twice at 20,450,880 bytes with SHA-256
`e66abf203481c5df0fa0fc0062c414876f9ef6428cd637de0795f821496c51a9`.
Candidate aliases remain as `ae2-addon-m3f-candidate.jar`,
`ae2-m3f-gallery-candidate.zip` and
`ae2-m3f-enabled-candidate-map-2026-08-09.tar.gz`. The hash-exact accepted
aliases are `ae2-addon-m3f-accepted.jar`, `ae2-m3f-gallery-accepted.zip` and
`ae2-m3f-enabled-accepted-map-2026-08-09.tar.gz`. Neither alias set is a
publication store. Machine contents, held items, fluids, live or activity-
specific state and accurate Drive LEDs are accepted non-goals, not deferred
work. The bounded 2026-08-09 acceptance of this exact JAR, gallery and archive
completes M3.

S1 implements the post-M3 structural step under the independently fail-closed
profile ID `ae2-cable-bus-structural`. Its bounded static
off/inactive/unlocked projection covers all 29 ordered native face-part
identities, both plane families, six P2P types and frequency pixels, legal
dense layouts, the exact supported facade state/material boundary and 30
native attached-device identities. Unknown compatible extension endpoints and
unsupported or malformed layouts retain atomic whole-cable-bus fallback.

Two clean builds with Eclipse Temurin `21.0.12+8` and Python `3.13.14`
reproduced the All the Mons 1.2.0-retargeted artifact set. Each build ran 448
Java tests (446 passed and two opt-in exporter tests were intentionally
skipped) and 167 passing Python tests; all gates passed:

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| Production JAR | 855,833 bytes | `5dad1cf654c13b5b0aa5411264104ff2f17b942b7d4c5def698d24c476951c39` |
| Sources JAR | 388,206 bytes | `9e294bcd04614132fd15270650d1f9e369a9491e2d13dd00d82f8e8060c2dcf2` |
| POM | 1,637 bytes | `967132ef80201099cfb1a798f03ff1ac37e0ac84a551694d7276ac20c7ccc136` |
| Gradle module metadata | 2,859 bytes | `8753a96f4d79e924058a17d8fa92c26e13ff7cf89ca2c1156c6f75f95d038c02` |
| Schema-10 gallery ZIP | 70,925 bytes | `66253309fd2cbe6b48c4ff621b71efa573b90a8d14b199205397df4d85d305e5` |
| Schema-10 `cases.json` | 4,207,895 bytes | `389a9b2b82dd16e3f4af82f9836e593770e404995a153218937908528c17dcee` |
| Appended S1 oracle | 198,162 bytes | `ac9a54cee9a20be18e71d6c9fe4f16b894827d43bb49cb4d0e56c673280cec39` |
| Legacy-upgrade oracle | 6,155 bytes | `cf0d86c440d1f89fc13f2b131f4f1534fb42363ebdc92580af826058297eb3d0` |
| Enabled map archive | 20,660,117 bytes | `0f57f33a205124c67069263cce0af8d74fa04343397317c4e491275df41558cb` |

The accepted schema-9 identities remain immutable: the 49,679-byte gallery
ZIP is
`21ceec072cc3263a41bdb81874e897d48d5a1ce5e1c7d3ac3c0de3063818ee6c`
and its 3,314,082-byte `cases.json` is
`75e6ba2f40631a95f20cfa00d7ca952e521bc2c7a4eb155926334a223a945f3a`.
S1 appends 28 cases and 360 anchors: 351 custom anchors own 37,518 triangles
across 96 resources and 2,093 material rows, while nine fallbacks own zero
triangles. Ten retained schema-9 positions gain 840 triangles across 21
resources and 70 material rows. The combined structural closure is 370
positions, 361 custom anchors, 38,358 triangles, 96 resources and 2,163
material rows.

Enabled schema 10 has 150 cases and 957 anchors. Its 940 custom anchors own
64,938 triangles, 16 fallbacks own zero triangles, 289 resources are selected
and the stone control brings selected output to 64,948 triangles. The exact
2026-08-11 isolated lifecycle observed this result in initial and restored
enabled cold/warm captures. All four share the byte-identical 5,657,463-byte
report SHA-256
`14aa3b46386bead1f656f9796305c0000e835e5948ae06367d947a3afe837723`
and 46-file comparable-manifest SHA-256
`e1e592faabd263e1b9bacce14d56577f330d1b5cbd80336f2bd1563d3f1b2a78`.

Native-structural-disabled and physical-stock cold/warm pairs were also
independently byte-identical and matched their exact analyzer contracts. The
fixture persisted through unload/reload, full restart, route isolation,
physical removal and restoration with `#m3f_builds=1`, `#s1_builds=1`,
`#stable=2` and `#failures=0`. Every capture pod had zero restarts and all init
containers exited zero; both render workers advanced by 650 and 747 CPU ticks.
Two 46-member deterministic enabled archives reproduced exactly at 20,660,117
bytes with SHA-256
`0f57f33a205124c67069263cce0af8d74fa04343397317c4e491275df41558cb`.
The owner visually accepted that exact JAR, gallery and map archive in BlueMap
on 2026-08-11. Candidate aliases `ae2-addon-s1-candidate.jar`,
`ae2-s1-gallery-candidate.zip` and
`ae2-s1-enabled-candidate-map-2026-08-11.tar.gz` remain hash-exact to accepted
aliases `ae2-addon-s1-accepted.jar`, `ae2-s1-gallery-accepted.zip` and
`ae2-s1-enabled-accepted-map-2026-08-11.tar.gz`. Both sets were verified on
`data-atm120`; the historical PVC was untouched. The final visual-review pod
was Ready with zero restarts and zero initializer failures, every exact route
activated, the fixture counters remained `1/1/2/0`, and `save-all flush`
completed before staging was scaled to zero. At that historical checkpoint S1
was the latest exact human-accepted local result. M4/M5 later became the
accepted successor and was published as immutable prerelease
`v0.1.0-alpha.1` on 2026-08-12.

## M4/M5 accepted local checkpoint

M4/M5 adds eight exact, independently fail-closed routes:

| Route | Exact static coverage |
| --- | --- |
| `appflux` | AppliedFlux's Flux Accessor face part and 20 exact FE cells in native and Extended Drives; ordinary JSON blocks stay ordinary resource-pack output |
| `merequester` | The requester block's 24 exact persisted variants, including `ae2:z` normalization, and the requester-terminal face part in its offline pose |
| `expandedae` | The I/O Port's 48 exact variants, both exact face parts, and 21 formed crafting blocks; the colorable Drive retains atomic stock fallback because upstream has no bounded world visual |
| `megacells` | Eight formed crafting blocks, three generic face parts, Cell Dock, and 67 exact cell items mapped to 37 models for the Cell Dock and both Drive types |
| `advanced-ae-quantum` | Eight quantum-computer block roles using exact saved state and a complete 3-by-3-by-3 appearance neighborhood, projected unpowered/light-zero |
| `advanced-ae-athena` | Same-state quantum-alloy connected textures through exact Athena 4.0.6 semantics, using deterministic static frame zero |
| `extendedae-matrix` | Six Assembler Matrix blocks, including connected matrix glass, preserving formed/shape structure while projecting power off |
| `extendedae-planes` | Active Formation and Smart Annihilation cable-bus planes using the accepted S1 plane/collar structure and a static off, no-spin projection |

The existing exact Extended Drive route is retained for ExtendedAE 2.2.35.
All eight M4/M5 routes require the exact AE2 core. ExtendedAE planes also
require S1 native structural support. A route-local artifact, resource,
registration, bake, callback, or linkage failure disables only that route;
an AE2 core failure blocks every dependent M4/M5 route, and an S1 failure
blocks only the ExtendedAE planes among them. The canonical `extendedae`
operator switch covers both ExtendedAE M4/M5 routes, while their internal
route IDs remain independently selectable for diagnostics.

This work renders static world structure, not network telemetry. Live power,
channels, activity, requested jobs, displayed stacks, inventories, cell or
machine contents, fluids, animations, and accurate LEDs remain non-goals.
Exact durable state and observed topology are preserved where supported;
missing, malformed, unknown, or unsupported observations use atomic original-
resource fallback. The exact schema-11 gallery and isolated lifecycle passed
technical validation. The owner completed the bounded BlueMap visual review
on 2026-08-12 and accepted the exact JAR, gallery, and map archive. The same
message separately authorized publication. The exact release-version build
was published as immutable prerelease `v0.1.0-alpha.1`.

## Applied Mekanistics alpha.2 release

Published prerelease `v0.1.0-alpha.2` adds one independently fail-closed route:

| Route | Exact static coverage |
| --- | --- |
| `appmek-drive-cells` | Five ordinary and five portable chemical cells mapped to five exact chassis models in native `ae2:drive` |

The route requires exact AppMek and Mekanism identities plus active native
Drive support. Unknown cells, malformed slot data, inactive dependencies, or
resource mismatch use whole-Drive stock fallback. A thrown AppMek model
callback disables only this route. Resource-pack overrides are validated
semantically before use. Chemical P2P, Extended Drive, ME Chest, and MEGA Cell
Dock integration are outside this candidate. The schema-12 seam cases exercise
the already-supported parent AE2 and Mekanism renderers and add no route.

Ordinary chunk data does not preserve live AE2 power, channel or connection
state. The profile therefore uses the explicit `idle-off-unknown` policy and
makes no live-network claim. Smart-cable overlays are the exact zero-channel
static representation, not telemetry.

The complete M0-M5 scope is human accepted. The exact `0.1.0-alpha.1` and
`0.1.0-alpha.2` non-SNAPSHOT artifacts completed the independent promotion
workflow: their protected tags, immutable GitHub prereleases, both JAR
attestations, checksum-closed assets, and Maven packages are published. The
`0.1.0-alpha.3` registration-hook candidate carries separate bounded staging
acceptance. Root workspace integration remains a separate coordination action.
This is an unofficial community add-on, is not affiliated with or endorsed by
BlueMap or Applied Energistics 2, and is not recommended for production.

## Architecture

```text
BlueMap add-on entrypoint
        |
exact BlueMap 5.22 adapter and inactive-by-default hooks
        |
exact AE2 19.2.17, S1 and nine All the Mons 1.2.0 extension/dependency gates
        |
bounded cable-bus, Drive, crafting-monitor and extension DTOs
        |
ten independently fail-closed extension routes plus accepted native routes
        |
deterministic cable, part, Drive, glass, crafting, quantum, connected-texture and matrix geometry
        |
direct original-resource fallback for every unsupported condition
```

The production JAR contains no AE2-family mod, Athena, Minecraft, NeoForge,
BlueMap or BlueNBT classes or assets. Soft-dependent BlueMap add-ons can use
the bounded [data-only extension API](docs/EXTENSION_API.md). See
[architecture](docs/ARCHITECTURE.md), [compatibility](docs/COMPATIBILITY.md),
[coverage](docs/COVERAGE.md), and [provenance](docs/PROVENANCE.md).

## Build

The default build expects the exact BlueMap backport in sibling directory
`../bluemap-backport`. The current M4/M5 gate is
`verifyM45PinnedArtifacts` with all nine `m45*Jar` inputs, including exact
AppMek and Mekanism JARs. Historical
M3b/M3d gates retain the old `extendedAeJar`/`advancedAeJar` properties; the
S1 gate deliberately uses the All the Mons 1.2.0 `nativeStructural*Jar`
properties:

```bash
python3 -m unittest discover -s tools/tests -p 'test_*.py' -v
./gradlew --no-daemon clean check build
./gradlew --no-daemon \
  -Pae2Jar=/absolute/path/appliedenergistics2-19.2.17.jar \
  verifyPinnedArtifact
./gradlew --no-daemon \
  -PextendedAeJar=/absolute/path/ExtendedAE-1.21-2.2.33-neoforge.jar \
  verifyExtendedAePinnedArtifact
./gradlew --no-daemon \
  -Pae2Jar=/absolute/path/appliedenergistics2-19.2.17.jar \
  verifyQuartzGlassPinnedArtifact
./gradlew --no-daemon \
  -Pae2Jar=/absolute/path/appliedenergistics2-19.2.17.jar \
  -PmegaCellsJar=/absolute/path/megacells-4.11.0.jar \
  -PexpandedAeJar=/absolute/path/expandedae-2.1.1.jar \
  -PadvancedAeJar=/absolute/path/advancedae-1.6.11-1.21.1.jar \
  -PextendedAeJar=/absolute/path/ExtendedAE-1.21-2.2.33-neoforge.jar \
  verifyCraftingPinnedArtifact
./gradlew --no-daemon \
  -Pae2Jar=/absolute/path/appliedenergistics2-19.2.17.jar \
  -Pae2SourcesJar=/absolute/path/appliedenergistics2-19.2.17-sources.jar \
  verifyQuantumBridgePinnedArtifact
./gradlew --no-daemon \
  -Pae2Jar=/absolute/path/appliedenergistics2-19.2.17.jar \
  -Pae2SourcesJar=/absolute/path/appliedenergistics2-19.2.17-sources.jar \
  verifyM3CompletionPinnedArtifact
./gradlew --no-daemon \
  -Pae2Jar=/absolute/path/appliedenergistics2-19.2.17.jar \
  -Pae2SourcesJar=/absolute/path/appliedenergistics2-19.2.17-sources.jar \
  -PmegaCellsJar=/absolute/path/megacells-4.11.0.jar \
  -PexpandedAeJar=/absolute/path/expandedae-2.1.1.jar \
  -PnativeStructuralAdvancedAeJar=/absolute/path/AdvancedAE-1.6.12-1.21.1.jar \
  -PnativeStructuralExtendedAeJar=/absolute/path/ExtendedAE-1.21-2.2.35-neoforge.jar \
  -PnativeStructuralGlassentialJar=/absolute/path/Glassential-renewed-1.21.1-3.4.5.jar \
  -PnativeStructuralBlueMapJar=/absolute/path/bluemap-5.22-agent.backport-5.22-mc1.21.1-2-neoforge.jar \
  -PminecraftClientJar=/absolute/path/minecraft-1.21.1-client.jar \
  verifyNativeStructuralPinnedArtifact
./gradlew --no-daemon \
  -Pae2Jar=/absolute/path/appliedenergistics2-19.2.17.jar \
  -Pm45AppFluxJar=/absolute/path/AppliedFlux-1.21-2.1.5-neoforge.jar \
  -Pm45MeRequesterJar=/absolute/path/merequester-neoforge-1.21.1-1.4.3.jar \
  -Pm45ExpandedAeJar=/absolute/path/expandedae-2.1.1.jar \
  -Pm45MegaCellsJar=/absolute/path/megacells-4.11.0.jar \
  -Pm45AdvancedAeJar=/absolute/path/AdvancedAE-1.6.12-1.21.1.jar \
  -Pm45AthenaJar=/absolute/path/athena-neoforge-1.21.1-4.0.6.jar \
  -Pm45ExtendedAeJar=/absolute/path/ExtendedAE-1.21-2.2.35-neoforge.jar \
  -Pm45AppMekJar=/absolute/path/Applied-Mekanistics-1.6.3.jar \
  -Pm45MekanismJar=/absolute/path/Mekanism-1.21.1-10.7.19.85.jar \
  verifyM45PinnedArtifacts
```

`check` runs the same standard-library-only Python analyzer tests in addition
to the Java gates.

## Runtime map evidence

After a completed isolated render, analyze the matching BlueMap file-map root:

```bash
python3 tools/analyze_prbm.py \
  --map-root /absolute/path/to/web/maps/ae2_staging \
  --output /tmp/ae2-staging-prbm-report.json
```

Schema 11 is the exact M4/M5 technical-review format. Its 158 cases and 1,366
anchors retain the accepted schema-10 S1 projection and append eight M4/M5
cases with 409 anchors. The 221,769-byte main oracle has SHA-256
`c2ce69bed949306551ca4ff6cdebf7fac88f0f2f2fa7ab294d3312f363e1b448`;
the 2,336-byte legacy-upgrade oracle has SHA-256
`2319ecf576ba07b123078c720d941990fac939033d375e5853f51bf98348c3c7`.
The isolated lifecycle produced these exact byte-identical cold/warm pairs:

| Mode/phases | Report | Report SHA-256 | 79-row manifest SHA-256 |
| --- | ---: | --- | --- |
| Initial and restored enabled cold/warm | 7,695,130 bytes | `6aaa925961ace51dfd2a7e074f9745d78e4aae28a334425f55498fa2bb5038fc` | `97a4f0915d6698e0962d6355f0835a2286a3ee7cae983ad1f3514c9ecb76b46f` |
| Combined-disabled cold/warm | 6,760,492 bytes | `acc180648556ba2a0fc4115172ba04aad4e0c266358379bf75f80f5353c62aa2` | `d457c23adc189c0e03718aad5c09649a7db63c1efb0174b439432df9f480ac56` |
| Crafting-disabled cold/warm | 5,956,875 bytes | `8b2b7b3b857c88f1c201399cffeeead588ac78e3ae3023d64ca4c3952a8b1593` | `dd86e95e7001ddb22e355f6910d7e7d3ded032d4eb2fcedc1a5ffd2deb1258cc` |
| Native-structural-disabled cold/warm | 5,697,202 bytes | `0742cc20f7fd7af776f1a342c52206e6ee6107c758146b9376358019b8a34a10` | `680ae50473f9829f396fc09f260e28221f283eda30a2b438688a81ca9788a668` |

Full-JVM transitions, restored equality, and the restart, initializer, worker,
one-build, and settle gates passed. The deterministic enabled map archive is
20,821,895 bytes with SHA-256
`44422aa71c2f450951d8433e25e01de7a0b00dbd0d9c4fa4ff74ca98e649a2df`.
This exact technical evidence underpins the owner visual acceptance recorded
above.

The accepted predecessor schema-10 report embeds the accepted schema-9
M0-M3f view byte-for-byte. Its enabled mode requires all 957 anchors, validates
940 custom anchors, 16 atomic fallbacks, 64,938 custom triangles and the exact
289-resource gallery closure; the stone control brings selected output to
64,948 triangles. The appended S1 oracle covers 351 custom anchors, 37,518
triangles, 96 resources and 2,093 material rows. Its separate legacy-upgrade
oracle covers ten retained positions, 840 triangles, 21 resources and 70
material rows. The M3f lane remains immutable and continues to check persisted
paint geometry, closed chest models, neutral crank/Inscriber transforms and
bounded pylon topology.

For that predecessor, extension-disabled mode empties the 36 M3b anchors;
`--glass-disabled` empties
the 47 M3c anchors; `--crafting-disabled` empties all 86 M3d anchors; and
`--quantum-disabled` empties all 27 M3e anchors while reproducing the frozen
schema-7 enabled totals. `--m3-completion-disabled` selects the exact stock
projection at all 78 M3f anchors while preserving the accepted schema-8 custom
slice and enabled S1. `--native-structural-disabled` retains 589 custom
anchors, 27,188 custom triangles, 218 resources, 17 fallbacks and 27,198
selected triangles. At the 360 appended positions it renders the exact
ten-anchor, 608-triangle/14-resource predecessor projection and leaves 350
empty; all ten legacy-upgrade positions are empty. Physical stock retains the
ten-triangle stone control, renders 38 M3f anchors and totals 1,882 selected
triangles across five resources. It leaves 918 anchors empty, including all
360 appended S1 anchors and all ten legacy upgrades. The 2026-08-11 isolated
captures matched these exact mode totals. Enabled/restored report and
46-file-manifest SHA-256 values are respectively
`14aa3b46386bead1f656f9796305c0000e835e5948ae06367d947a3afe837723`
and
`e1e592faabd263e1b9bacce14d56577f330d1b5cbd80336f2bd1563d3f1b2a78`;
native-structural-disabled values are
`63e528d6aa3c033cd6b2251f7a569cc0e4e7dc4bfba81d75862f8fa7a416e274`
and
`7cb0b48aa938109d8c001d32f01375aba7f29f7f9b8c5f96a927b52335a7df03`;
physical-stock values are
`a1e148fde5af118def7e379c44a2294ca5d9485a60824ad2764911f1805788a4`
and
`9539d160733a7616ec9092e82086539b4db725e0afab2b04f5111a77e0da66f0`.
PRBM does
not retain renderer identity, block NBT, Crafting Monitor transient display
content, quantum particles or AE2's `shade=false` flag, so a report cannot
prove renderer provenance, live power or pixel-identical client lighting.
S1's non-lighting invariant excludes only environment-derived blocklight and
sunlight, while full observed attribute signatures retain both; every
triangle requires flat in-range light and the four smart-channel resources
remain fullbright. See
[staging](docs/STAGING.md) for the exact technical evidence boundary.

## Installation and rollback

No production installation is recommended. In isolated staging, the add-on
JAR belongs in BlueMap's `config/bluemap/packs` directory and requires a full
JVM restart. Rollback is stop, remove this one JAR, and restart. The add-on
writes no required world state.

## License

`LGPL-3.0-only`. AE2-family mod and Athena models, textures, classes, and JARs
remain operator-installed and are not redistributed. See `NOTICE.md`,
`THIRD_PARTY.md`, and
`provenance/upstreams.json`.
