# AE2-family M4/M5 schema-11 staging gallery

This directory contains the deterministic datapack and exact schema-11
technical evidence contract for the owner-accepted All the Mons 1.2.0 M4/M5
checkpoint.
It retains the accepted schema-10 S1 projection and appends eight independently
fail-closed extension routes across AppliedFlux, ME Requester, Expanded AE,
MEGA Cells, Advanced AE/Athena, and ExtendedAE. This is development evidence
for an isolated synthetic world, not gameplay content or a production
migration tool.

The 94,537-byte candidate gallery ZIP has SHA-256
`c67b4f794092f6e994349a8ee9320c052e2efc87f04e8813faf158c3455fe33b`;
its 6,017,554-byte `cases.json` has SHA-256
`914dab6931077521959cf59260a1ffb0cdbe105385f43880763b289f8117ec55`.
The 221,769-byte main runtime oracle and 2,336-byte schema-10 legacy-upgrade
oracle have SHA-256 values
`c2ce69bed949306551ca4ff6cdebf7fac88f0f2f2fa7ab294d3312f363e1b448`
and `2319ecf576ba07b123078c720d941990fac939033d375e5853f51bf98348c3c7`.
The isolated technical lifecycle passed, and the owner accepted the exact JAR,
gallery, and deterministic map archive in BlueMap on 2026-08-12.

The generated datapack keeps the single cumulative load namespace `ae2_m3`.
The former generated `ae2_m2` namespace remains deliberately absent, and M3f
through M5 do not introduce another namespace: the existing build, verify,
pose, release, and dense-regression command contract stays stable. S1 retains
`ae2s1run/#s1_builds`; M4/M5 adds `ae2m45run/#m45_builds`. Both one-build
counters leave the retained M3f counter unchanged.

## Schema-11 gallery contract

The 158 logical cases contain 1,366 anchors. The eight M4/M5 cases append 409
anchors to the exact 150-case/957-anchor schema-10 predecessor. Of the appended
anchors, 391 are custom and 18 are atomic fallbacks. The main runtime oracle
locks those 391 custom anchors, 23,334 triangles, 122 resources, and 2,089
material rows. A separate legacy-upgrade oracle locks three retained schema-10
positions, 282 triangles, 20 resources, and 26 material rows.

The eight appended cases correspond one-to-one with `appflux`, `merequester`,
`expandedae`, `megacells`, `advanced-ae-quantum`, `advanced-ae-athena`,
`extendedae-matrix`, and `extendedae-planes`. Their disabled projections are
route-owned; the combined-disabled projection has 299 nonempty and 110 empty
M4/M5 anchors, while the declared physical-stock projection reverses that
split. Crafting-disabled empties all 20 directly affected observer anchors.
Native-structural-disabled blocks only the ExtendedAE plane dependency among
the eight routes.

## Historical predecessor checkpoint summary

Historical artifact identities and acceptances remain tied to their exact
gallery versions; M3c was accepted independently rather than inheriting an
earlier milestone's decision.

The exact 26,633-byte M3a gallery ZIP (SHA-256
`91057d94890bc3bd063fb7c4bf951f9a70b62a00ee01a9b00deb0fd8f674bb2b`)
and matching 259,005-byte M3a SNAPSHOT JAR (SHA-256
`55a11805373aebfde821e5009723ec7d672fb290127dbc60131ffa344c99518a`)
passed their source/build, persistence, enabled, physically add-on-absent
stock, restoration, and deterministic-evidence lifecycle. The owner visually
accepted those exact M3a artifacts on 2026-08-07. That acceptance closes M3a
only.

The accepted M3b build inputs produce a deterministic 37,614-byte gallery ZIP
with SHA-256
`69bdb99d9c8f6838c3b8d5847c32702761cfa77b263ee95384ca24357c84cf92`.
The paired local JAR is 323,416 bytes with SHA-256
`f02123cb602bb7b6466d1529c5518e45862f53f413ce9a75ecc067d1a30607d1`.
The complete technical runtime lifecycle passed, and on 2026-08-07 the owner
visually accepted these exact artifacts plus the exact 20,351,418-byte map
archive with SHA-256
`c73844990847148d9cd3d315832085e49776e9253c5c8eca6f0b7659d73c4285`.
They remain local and unpublished. The exact coordinated M3c schema-6
artifacts passed their complete technical lifecycle and human visual
acceptance on 2026-08-08. M3c is complete. The exact M3d JAR, gallery and map
archive subsequently passed the complete technical lifecycle and were visually
accepted on 2026-08-08. The exact M3e candidate passed its reproducible build
and complete isolated technical lifecycle on the same date, and the owner
visually accepted its exact JAR, gallery and archive. M3e was the latest
human-accepted slice and rollback checkpoint at that stage; M3d remained the
previous rollback checkpoint. The combined M3f implementation and isolated
technical lifecycle are complete for the exact artifacts recorded below. The
owner visually accepted the exact corrected M3f JAR, gallery and map archive
on 2026-08-09, completing M3. Candidate and accepted M3f aliases now coexist
hash-exactly.

## Embedded schema-10 predecessor contract

The first 150 cases contain 957 anchors. The first 122 cases and 597 anchors
are the byte-frozen accepted schema-9 M0--M3f view. The 28 appended S1 cases
contain 360 anchors. Within the retained slice, the first 115 cases and 519 anchors
are the byte-frozen accepted schema-8 M0--M3e view; within that slice, the
first 112 cases and 492 anchors are the accepted schema-7 M0--M3d view; within that slice, the
first 103 cases and 406 anchors are the accepted schema-6 M0--M3c view; within that slice, the
first 92 cases and 359 anchors are the frozen schema-5 M3b regression data,
and the first 62 cases and 290 anchors are the complete accepted M1/M2
regression gallery without fixture-semantic changes:

- one vanilla stone control and two whole-block device-endpoint fallbacks;
- all 85 center-cable IDs, straight lines, junctions, compatible family pairs,
  incompatible color pairs, and the optional dense regression fixture;
- all accepted M2 terminal face/spin, multipart, topology, and canonical
  same-face stone-facade cases; and
- all nine retained M2 atomic fallback cases.

Those retained 278 custom anchors still own exactly 8,576 triangles. The 11
retained stock-fallback anchors still own zero triangles.

The accepted, disjoint M3a Drive region retains 14 cases and 33 anchors:

- six orientation cases containing all 24 `facing`/`spin` states, each with
  one `ae2:item_storage_cell_1k` in slot 0;
- one empty Drive;
- three catalog Drives covering all 23 exact supported item IDs;
- one sparse slot 0/9 Drive and one full ten-slot mixed Drive;
- one two-anchor component-insensitivity case; and
- one durable MEGA Cells atomic fallback.

Its 32 custom anchors own exactly 3,856 triangles, select all 23 supported AE2
cell IDs and all 12 occupied chassis models, and retain the exact `90 + 16N`
formula. Its one unsupported anchor owns zero triangles. Across M1 through
M3a, 310 custom anchors own 12,432 triangles and select 159 runtime materials,
including structurally resolved vanilla stone; 12 atomic fallbacks own zero.

The new, disjoint M3b Extended Drive region adds 16 cases and 36 anchors:

- six orientation cases containing all 24 `facing`/`spin` states, each with
  one occupied front slot and one occupied back slot;
- one empty Extended Drive;
- one front/back catalog for the three native ExtendedAE cells;
- one sparse first/last-slot case spanning both faces;
- one full 20-slot mixed Extended Drive;
- a two-anchor front/back mirror case;
- a two-anchor component-insensitivity case; and
- four durable atomic fallbacks for a MEGA Cells item, a pack KubeJS item, a
  supported AE2 cell with invalid count 2, and `minecraft:stone`.

The 32 custom M3b anchors own exactly 5,056 triangles and contain 84 occupied
slots:

| Fixture | Anchors | Occupied slots | Triangles |
| --- | ---: | ---: | ---: |
| 24 full-orientation Extended Drives | 24 | 48 | 3,552 |
| empty Extended Drive | 1 | 0 | 116 |
| native front/back catalog | 1 | 6 | 212 |
| sparse first/last slots on both faces | 1 | 4 | 180 |
| full 20-slot Extended Drive | 1 | 20 | 436 |
| front/back mirror pair | 2 | 2 | 264 |
| component-insensitivity pair | 2 | 4 | 296 |

Every supported Extended Drive has exactly `116 + 16N` triangles for `N`
occupied slots. The ExtendedAE base model contributes 116 triangles. Each
occupied chassis contributes six triangles and its static offline-unknown LED
contributes ten. The ExtendedAE renderer emits those LEDs as position/color
geometry without a texture; the evidence contract records them through the
shared `ae2:block/drive/drive_front` material proxy used by the BlueMap
writer/analyzer. `extendedae:block/generics/front` belongs only to the base
model and is not asserted as an LED texture.

Slots 0 through 9 are on the front face and slots 10 through 19 are on the
back. Back slots use the opposite facing with the same spin. Schema 5 records
the exact global slot, face-local slot, face, orientation, model, material,
and origin for every position. The supported catalog is deliberately bounded
to the 23 M3a AE2 IDs plus exact ExtendedAE
`infinity_water_cell`, `infinity_cobblestone_cell`, and `void_cell`. The four
unsupported or malformed anchors exercise atomic whole-block fallback; this
does not claim support for every pack-registered KubeJS or extension cell.

Across M1 through M3b, 342 custom anchors own 17,488 triangles and select 167
runtime materials, including resolved vanilla stone. All 16 atomic fallback
anchors must own zero triangles. The one stone control remains separate.

The disjoint M3c region adds 11 representative connected-glass cases and 47
custom anchors for both native variants. It covers isolated matched-selection
pairs; axial and corner connections; three-, four- and six-neighbor forms;
diagonal-only separation; a 3-by-3 checkerboard plane; a 2-by-2-by-2
checkerboard cube; and opaque-neighbor culling. The selected mesh is exactly
776 triangles using four position-selected base resources and all 15 nonzero
frame-mask resources. Every mask from `0001` through `1111` occurs, and two
checkerboard-center faces demonstrate `0000` as base-only output with no frame.
Each face's listed local directions are numeric bit positions `0..3`: an
absent neighbor sets `1 << index`, and the resulting integer is formatted as
four-bit binary. The direction list is therefore not concatenated as displayed
left-to-right; for example, the up/down-connected anchor at `(214,100,290)`
uses `1010` on each of its four visible faces.

This gallery is a representative visual/topology matrix, not exhaustive
coverage of all ten rotational connection-count orbits. The add-on's Java
geometry tests remain responsible for all 64 six-neighbor masks, including
the connection-count-five cases that are intentionally not added here.
For every visible face the exact formula is
`2 * visibleFaces + 2 * visibleFrameFaces`; shared connected faces and the
east face behind the case-11 context stone must be absent.

Across M1 through M3c, 389 custom anchors own 18,264 triangles and select 186
resolved runtime resources. The 16 frozen atomic fallbacks still own zero
triangles, and the one stone control remains separate.

The disjoint M3d region adds nine cases and 86 anchors:

- an isolated catalog of the five native AE2 crafting-storage sizes;
- unit/storage and accelerator/storage pairs, a three-block axis line and a
  2-by-2 plane;
- a genuinely formed and powered 2-by-2-by-2 CPU, supplied by an unselected
  creative-energy-cell and cable-bus context stack;
- an unpowered 3-by-3-by-3 hard-culling CPU whose fully enclosed center emits
  no geometry;
- 17 monitor/storage pairs covering painted-color ordinals `0..16`, all six
  facings and all four spin values with the monitor front exposed; and
- a native 1k storage anchor between compatible MEGA Cells and Expanded AE
  crafting units, which must use one atomic whole-block fallback.

The 85 custom M3d anchors own exactly 4,306 triangles and select the exact
15-resource crafting closure. The 84 nonzero custom anchors prove route
activity; the enclosed center at `(305,101,270)` is still part of the custom
topology contract but cannot be distinguished from stock fallback by PRBM
output alone. It is therefore reported explicitly as
`fully-enclosed-zero-geometry`, with renderer provenance status
`not-renderer-provenance-distinguishable-in-prbm`. The mixed-extension anchor
is the only new fallback and owns zero triangles.

Across M1 through M3d, 474 custom anchors own 22,570 triangles and select 201
resolved runtime resources. All 17 atomic fallbacks own zero triangles, and
the separate stone control owns its stock ten-triangle mesh.

The disjoint M3e region adds three complete native quantum bridges and 27
custom anchors. Each bridge is a settled, dry, unpowered 3-by-3 plane with one
center `ae2:quantum_link`, four edge rings and four corner rings. The gallery
contains one bridge in each XZ, XY and YZ orientation; the XZ structure spans
the `x=287/288` chunk boundary. Every bridge owns exactly 396 static-off
triangles: 108 at the link, 36 at each corner and 36 at each edge. Its material
totals are 12 link, 48 transparent glass-cable, 144 transparent covered-cable
and 192 ring triangles.

Across M1 through M3e, 501 custom anchors own 23,758 triangles and select 203
resolved runtime resources. The quantum route emits four resources, but only
`ae2:block/quantum_link` and `ae2:block/quantum_ring` are new to the cumulative
closure. The 17 retained atomic fallbacks still own zero triangles, and the
stone control remains separate. Transient powered/QES overlays and particles
are intentionally omitted under the `static-off-unknown` policy.

The disjoint M3f region adds seven cases and 78 anchors. Twenty-three paint
anchors cover all 16 persisted colors, all six absolute backing faces and a
three-layer sample (25 splotches / 50 triangles). Eight closed Sky Stone chest
anchors cover both variants and all four horizontal facings. Six neutral
cranks cover all attachment facings with persistent charger context. Twenty-four
neutral empty Inscribers cover every facing/spin transform. Seventeen spatial
pylons cover isolated plus start/middle/end roles on X, Y and Z and every
member of complete invalid three-block L and four-block T components, which
render with the unformed BASE-plus-DIM appearance.

The 78 custom M3f anchors own exactly 2,822 triangles and emit 15 new static
textures. Cumulatively, 579 custom anchors own 26,580 triangles and select 218
resolved resources; 17 atomic fallbacks own zero triangles and the stone
control remains separate. Machine contents, held items, live animation/state,
fluids and accurate Drive LEDs remain deliberate non-goals.

All Drives remain deliberately disconnected and unpowered. The gallery tests
the exact static-offline-unknown representation and makes no claim about live
network state.

## S1 native structural append

The disjoint S1 region adds 28 representative cases and 360 anchors under
coverage ID `s1-native-structural` and route/profile ID
`ae2-cable-bus-structural`. Of those, 351 are custom anchors owning exactly
37,518 triangles and selecting 96 S1 semantic resources, 71 new to the
cumulative gallery, across 2,093 material rows. Nine persistent controls use
atomic whole-cable-bus stock fallback and must emit no custom triangles.

S1 also upgrades ten retained schema-9 positions through a separate legacy
oracle: all ten become custom and own 840 triangles across 21 resources and 70
material rows. The appended and legacy-upgrade union therefore contains 370
positions, 361 custom anchors, 38,358 triangles, 96 resources and 2,163
material rows. Cumulatively, enabled schema 10 has 940 custom anchors, 64,938
custom triangles, 289 selected resources and 16 zero-triangle fallbacks; the
stone control brings selected output to 64,948 triangles.

The nine physical fallback anchors are seven native reporting-part controls
with persisted `spin=4` (one shared invalid-reporting-spin mechanism), one
`minecraft:oak_stairs` non-full-cube facade, and the exact pack-pinned Expanded
AE `expandedae:exp_io_port` known-compatible endpoint whose connection outcome
is `UNKNOWN`. The fallback substitution itself changes the predecessor-mode
projection described below. Because all nine anchors own zero custom output,
it does not change the enabled 351/9 custom/fallback split or its triangle and
resource totals; the revised enabled totals instead record the final custom-
render oracle and resource closure.

The bounded visual matrix covers:

- all 29 ordered native face-part identities on all six installed faces, with
  representative spin cycling for the nine reporting parts;
- all 16 coplanar masks for each annihilation and formation plane, preserving
  the persisted bit order `UP=8`, `RIGHT=4`, `DOWN=2`, `LEFT=1`;
- all six P2P types and representative unsigned-short frequencies `0`,
  `0x1234` and `0xffff`, including exact frequency-pixel material output;
- legal dense cable-anchor layouts, multipart and part-only buses;
- all 64 facade face masks plus opaque, transparent, foliage-tinted, animated-
  texture and persisted-property material/state examples, same-face part
  clipping, adjacent-face stripping, short stilt support and coexistence; and
- all 30 native attached-device identities in exact profile registry order,
  their declared/effective cable families, collar policy, known disconnected
  neighbors and the exact compatible-`UNKNOWN` extension control.

Illegal dense-part layouts, missing P2P frequency data, malformed facade data,
facade-only buses and retained-field/budget-overflow cases remain Java/DTO
synthetic fail-closed evidence. They are not claimed as live physical S1
gallery fallbacks.

The Java suite remains authoritative for the exhaustive 336 unique part
orientation states (`20*6 + 9*6*4`) and the full Cartesian combinations. S1
intentionally renders static off/inactive/unlocked structure only. Contents,
held items, fluids, activity-specific state and exact Drive LEDs remain the
accepted non-goals.

The oracle-independent raw S1 matrix and rendered logical projection are
source-audited independently from the runtime export. The exact 198,162-byte
schema-2 appended oracle has SHA-256
`ac9a54cee9a20be18e71d6c9fe4f16b894827d43bb49cb4d0e56c673280cec39`
and locks 351 anchors, 37,518 triangles, 96 resources and 2,093 material rows.
The exact 6,155-byte legacy-upgrade oracle has SHA-256
`cf0d86c440d1f89fc13f2b131f4f1534fb42363ebdc92580af826058297eb3d0`
and locks ten anchors, 840 triangles, 21 resources and 70 rows. Separate hard-
coded representative goldens and field-by-field mutations prevent a self-
referential-only check.

## Bounds and persisted NBT

The unchanged M1 fixture is bounded to `x=208..263`, `y=99..104`,
`z=192..239`. The unchanged M2 fixture remains at `x=208..239`,
`y=99..104`, `z=242..249`. M3a owns `x=240..263`, `y=98..104`,
`z=242..249`. M3b owns the separate volume `x=240..279`, `y=98..104`,
`z=260..267`. M3c owns `x=208..279`, `y=97..104`, `z=288..307`. M3d owns
`x=296..319`, `y=97..105`, `z=260..299`; all selected and context blocks are
at `x>=297` and remain more than 15 blocks from M3c vibrant glass.
M3e owns the disjoint air-isolated volume `x=281..294`, `y=97..105`,
`z=269..278`.
M3f owns `x=280..319`, `y=96..106`, `z=208..230`. Its paint backing blocks
and crankable charger contexts are structural fixtures, not selected anchors.
S1 owns the disjoint air-isolated volume `x=208..319`, `y=96..110`,
`z=312..367`. Its clear is split into four bounded fill operations below the
Minecraft command-volume limit. S1 has no support floor; ordinary host light
uses skylight 15/blocklight 0 in the plains staging biome, while explicit
emissive helpers retain their declared light policy.
M4/M5 owns the disjoint volume `x=336..511`, `y=96..110`, `z=312..431`.
Its anchors are at `y=100` and are air-isolated except for exact powered-I/O
network helpers. Formed fixtures use only live-proven physical layouts and
fail closed if settling rewrites their required state.

Both Drive regions use smooth-stone support at `y=98`, explicitly cleared air
at `y=99`, and anchors at `y=100`. The one-block air gap is part of the
evidence contract: direct support culls Drive model faces and invalidates the
triangle formulas.

The M3c support floor is at `y=97`; `y=98` and `y=99` are explicitly air as
needed, with primary case centers at `y=100`. The opaque stone at
`227 100 301` is context-only and deliberately is not an analyzer anchor.

The M3d support floor is also at `y=97`. The volume from `y=98` through
`y=105` is normally cleared; only the powered CPU's creative-energy-cell and
exact cable-bus context stack occupy its otherwise empty vertical context.
The generated load/release functions own precisely the chunks covering
`296,260` through `319,299` in addition to the frozen earlier areas. The M3e
volume has no support floor: its complete bridge structures are surrounded by
air, so neighbor-derived runtime AO evaluates to raw 255 in this fixture. This
is an expected fixture outcome, not a constant-AO renderer policy.
Load/release also owns the M3e
chunks, including both sides of the explicit x-boundary fixture.

The observation deck remains `x=214..228`, `y=106..110`, `z=251..257`, with
guarded pose `221.5 108 254.5`, yaw `180`, pitch `14`. The FramedBlocks
controller sentinel remains in `x=255..258`, `y=99..102`, `z=255..257`.

Each M3a Drive has its exact `ae2:drive[facing=<direction>,spin=<0..3>]`
state and deterministic `inv.item0` through `inv.item9` compounds. Each M3b
Extended Drive has its exact
`extendedae:ex_drive[facing=<direction>,spin=<0..3>]` state and all 20
`inv.item0` through `inv.item19` compounds. Verification checks every block
state, inventory field, ID, count, component expectation, empty retained slot,
support block, and air-gap block; it also rejects an unexpected `item20`.

The M3a component and MEGA Cells payloads passed the accepted delayed,
chunk-unload/reload, and full-JVM persistence lifecycle. A bounded live probe
also showed historical ExtendedAE 2.2.33 retaining both a count-2 AE2 stack and a
`minecraft:stone` stack through `save-all flush`, actual chunk unload, and
reload. That precursor supported keeping both M3b fallback cases. The later
full M3b lifecycle then retained those states, the other four critical probes,
zero verifier failures and exactly one fixture build through unload/reload and
complete JVM transitions.

## Generate, verify, and package

```bash
python3 gallery/generate.py
python3 gallery/generate.py --check
cd gallery
sha256sum --check SHA256SUMS
./package.sh /tmp/ae2-m5-gallery.zip
```

`SHA256SUMS` covers every generated file other than itself. The generator
checks profile and extension-artifact identity, explicit milestone and route
metadata, case/anchor uniqueness, disjoint mutation bounds, the complete
M1/M2/M3a regression closure, all 24 states for both Drive routes, exact
ten-slot and twenty-slot inventories, all 26 supported Extended Drive IDs,
front/back transforms, materials, LED attributes, fallbacks, and triangle
totals. Schema 6 checks the exact M3c positions, Java-compatible
position RNG, asymmetric base UV crop, visible-face culling, local four-bit
frame masks, all 19 selected glass resources and the 776-triangle closure.
Schema 7 additionally checks the exact M3d cuboids, stable block states,
formed/powered values, monitor facing/spin/paint, topology, materials, UVs,
colors, AO, light policy, 4,306-triangle closure and route isolation. The
schema-8 append checks the three complete quantum topologies, exact cuboids,
bounds-mapped UVs, materials, CubeBuilder winding, white CUTOUT color, AO 255,
ordinary directional world light, 1,188-triangle closure, chunk-boundary
coverage and the byte-frozen schema-7 projection. Schema 9 additionally checks
the exact 256-byte paint payloads, closed chest and neutral machine matrices,
local pylon topology, complete invalid-component unformed projection,
2,822-triangle closure, M3f bounds and the byte-frozen
schema-8 projection. The analyzer independently locks every M3f triangle at
q16 position/UV precision, preserving winding, encoded cardinal normals,
material, RGB, AO and one valid world-light pair per transformed face.
Schema 10 additionally validates the full S1 logical matrix, exact ordered
part and endpoint catalogs, plane/P2P/dense/facade/topology semantics, the
independent runtime oracle and the byte-frozen schema-9 projection.
Schema 11 additionally validates the eight M4/M5 cases, all 409 appended
anchors, route-owned enabled/disabled projections, the exact 391-anchor main
runtime oracle, the three schema-10 legacy upgrades, and the frozen schema-10
predecessor projection.
The quantum append also locks static-off/particle-omission policy. The packager
fixes entry order and timestamps, validates the ZIP, and prints its SHA-256.

The accepted schema-7 M3d datapack remains bound to its exact 44,201-byte ZIP
with SHA-256
`4a18b45f2c03c8d1d3c49a731df2c2503745952faccf9ba06ec8f301909b81f3`.
The final schema-8 M3e datapack packaged byte-identically twice at 45,009 bytes
with SHA-256
`498bac2f82b78451eb24da416ded1d625e5785cc5d3e5910b4c34bfecc05c390`
after the complete synthetic gate. The owner subsequently accepted that exact
local artifact; it is not a release. Its technical runtime lifecycle is
recorded below.
The final schema-9 M3f datapack packaged byte-identically at 49,679 bytes with
SHA-256
`21ceec072cc3263a41bdb81874e897d48d5a1ce5e1c7d3ac3c0de3063818ee6c`.
Its exact 3,314,082-byte `cases.json` has SHA-256
`75e6ba2f40631a95f20cfa00d7ca952e521bc2c7a4eb155926334a223a945f3a`.
The owner visually accepted that exact gallery together with the exact JAR and
map archive on 2026-08-09. It remains a local artifact, not a release.
The accepted schema-6 M3c ZIP remains separately bound to
SHA-256 `0839009fe6a4f4785f864f33bc97fef28b8418f077d5d66a20efc3e8eeb4edab`.

The gallery was revalidated for All the Mons `1.2.0` at pack commit
`c7bb230f21d14d26859d0b92548f089b3a493ad9`, NeoForge `21.1.248` and the
canonical BlueMap `5.22-agent.backport-5.22-mc1.21.1-2` host, with Advanced
AE `1.6.12-1.21.1`, ExtendedAE `1.21-2.2.35-neoforge` and Glassential
`3.4.5` as the current S1 evidence inputs. Two clean Eclipse Temurin
`21.0.12+8` / Python `3.13.14` builds reproduced the local S1 artifact set.
Each ran 448 Java tests (446 passed and two opt-in exporter tests
were intentionally skipped) and 167 passing Python tests; all gates passed. The
855,833-byte production JAR has SHA-256
`5dad1cf654c13b5b0aa5411264104ff2f17b942b7d4c5def698d24c476951c39`,
the 388,206-byte sources JAR has SHA-256
`9e294bcd04614132fd15270650d1f9e369a9491e2d13dd00d82f8e8060c2dcf2`,
the 1,637-byte POM has SHA-256
`967132ef80201099cfb1a798f03ff1ac37e0ac84a551694d7276ac20c7ccc136`,
the 2,859-byte Gradle module metadata has SHA-256
`8753a96f4d79e924058a17d8fa92c26e13ff7cf89ca2c1156c6f75f95d038c02`,
the 70,925-byte schema-10 gallery ZIP has SHA-256
`66253309fd2cbe6b48c4ff621b71efa573b90a8d14b199205397df4d85d305e5`
and its 4,207,895-byte `cases.json` has SHA-256
`389a9b2b82dd16e3f4af82f9836e593770e404995a153218937908528c17dcee`.
The exact runtime lifecycle passed on 2026-08-11. Two deterministic enabled
candidate map archives reproduced byte-for-byte at 20,660,117 bytes with
SHA-256
`0f57f33a205124c67069263cce0af8d74fa04343397317c4e491275df41558cb`.
Each contains the same sorted 46-file comparable set, whose extracted manifest
matches the retained enabled/restored manifest exactly. The owner visually
accepted that exact JAR, gallery and map archive in BlueMap on 2026-08-11.
Candidate aliases `ae2-addon-s1-candidate.jar`,
`ae2-s1-gallery-candidate.zip` and
`ae2-s1-enabled-candidate-map-2026-08-11.tar.gz` are hash-exact to accepted
aliases `ae2-addon-s1-accepted.jar`, `ae2-s1-gallery-accepted.zip` and
`ae2-s1-enabled-accepted-map-2026-08-11.tar.gz` on `data-atm120`; the
historical PVC was untouched. These identities are accepted local-checkpoint
evidence, not release artifacts.

The final All the Mons 1.2.0 M4/M5 build artifacts reproduced byte-for-byte in
two builds. Each build ran 562 Java tests (560 passed and two intentional
exporter tests were skipped) and 180 pre-oracle Python tests. After freezing
the two oracles, the exact CPython 3.13.14 suite passed 192/192 tests in
945.343 seconds, and the generator/checksum closure passed. The exact local
artifacts are:

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| Production JAR | 1,207,683 bytes | `6fed7a625b02229213a047788995944f14e7e7fcabe0e0ddc6d9b5e994146e9f` |
| Sources JAR | 532,979 bytes | `4a45c60f8512630c6bd9735e26018d019ebe99d58f2c87fa2f3c46e101b624d8` |
| POM | 1,637 bytes | `967132ef80201099cfb1a798f03ff1ac37e0ac84a551694d7276ac20c7ccc136` |
| Gradle module metadata | 2,861 bytes | `3f0ba24c34ef535c99cbd6dabcd7d6bb0f784ca6ffd032f06faaf9a9b5d7b0b8` |
| Schema-11 gallery ZIP | 94,537 bytes | `c67b4f794092f6e994349a8ee9320c052e2efc87f04e8813faf158c3455fe33b` |
| Schema-11 `cases.json` | 6,017,554 bytes | `914dab6931077521959cf59260a1ffb0cdbe105385f43880763b289f8117ec55` |
| Enabled map archive | 20,821,895 bytes | `44422aa71c2f450951d8433e25e01de7a0b00dbd0d9c4fa4ff74ca98e649a2df` |

The candidate aliases are `ae2-addon-m5-candidate.jar`,
`ae2-m5-gallery-candidate.zip`, and
`ae2-m5-enabled-candidate-map-2026-08-11.tar.gz` on `data-atm120`. Hash-exact
accepted aliases are `ae2-addon-m5-accepted.jar`,
`ae2-m5-gallery-accepted.zip`, and
`ae2-m5-enabled-accepted-map-2026-08-11.tar.gz`.

Install `datapack/` only in the disposable AE2 staging world. The load tag
only force-loads the bounded main areas; it never clears or rebuilds them.
Building remains an explicit operator action:

```text
function ae2_m3:preprobe
function ae2_m3:build
function ae2_m3:verify
function ae2_m3:pose_south
function ae2_m3:release
```

`preprobe` is a bounded precursor that touches only the M3d powered-context
stack and mixed-extension line. It never calls the full build and never
modifies an M0--M3c anchor. Its dedicated `ae2m3p` scoreboard reports
`#result=1` only after two consecutive exact formed/powered checks, and
`#result=-1` on timeout.

Once that precursor is accepted, call the full `build` exactly once. The
generated scheduler waits for asynchronous native and extension structures to
settle, requiring two consecutive exact checks before saving and verifying.
The persistent `#m3f_builds ae2m3run`, `#s1_builds ae2s1run`, and
`#m45_builds ae2m45run` scores each increment exactly once per full build,
survive load/clear/settle/release, and must each equal one during verification;
a second full build is therefore detectable. The preprobe never touches these
objectives. The build never injects NBT or fabricates `formed`/`powered` state.
A later manual `verify` remains useful.
For the persistence gate, run `save-all flush`, call `ae2_m3:release`, keep
players away until the chunks unload, call `ae2_m3:load` without calling
`build`, and run `verify`. Repeat that non-rebuilding check after a full JVM
restart. A datapack reload is safe because the load function is force-load
only and non-mutating.

## Opt-in M1 dense regression

The optional four disjoint 8-by-4-by-8 fluix covered-dense lattices remain an
M1 regression. Their 1,024 cells own exactly 63,488 triangles: 12,288 core
triangles using `ae2:part/cable/core/dense_smart/transparent` and 51,200
connection triangles using `ae2:part/cable/dense_covered/transparent`.

The load function never builds them. Use the current namespace explicitly:

```text
function ae2_m3:dense/build
function ae2_m3:dense/verify
function ae2_m3:dense:clear
function ae2_m3:dense/release
```

The build uses four scheduled batches. `dense/release` removes only the
dense-exclusive force-loads; chunks shared with the main gallery remain owned
by `ae2_m3:load`.

## PRBM evidence boundary

Schema-11 `cases.json` embeds the canonical 4,207,895-byte accepted schema-10
view at SHA-256
`389a9b2b82dd16e3f4af82f9836e593770e404995a153218937908528c17dcee`.
It adds exactly eight cases/409 anchors and requires both exact M4/M5 oracles.
The embedded schema-10 view in turn embeds the canonical 3,314,082-byte
accepted schema-9 view at SHA-256
`75e6ba2f40631a95f20cfa00d7ca952e521bc2c7a4eb155926334a223a945f3a`.
That schema-9 view embeds the canonical 3,123,572-byte accepted schema-8 view at SHA-256
`93963dd0bb60a276e1a17c6dd1f4eb916cd92bef4ef30a2e8bdc7a2bfa818b3e`.
That view in turn embeds the accepted schema-7 contract at SHA-256
`c60d2afff5a1f92da4972963fcb926c38093f43bb6d7f550799f104349728a38`.
The analyzer rejects any schema-11 input that does not reproduce the embedded
frozen schema-10 view and all earlier projections, and emits complete schema-9
through schema-6 regression reports.
Every case carries an explicit milestone and renderer route; selection does
not infer ownership from a case name.

Enabled schema 10 validates 351 appended S1 custom anchors, 37,518 appended
custom triangles, 96 S1 resources (71 new), 2,093 material rows and nine S1
zero-triangle fallbacks. It separately validates ten legacy upgrades with 840
triangles, 21 resources and 70 material rows. The combined structural slice is
370 positions, 361 custom anchors, 38,358 triangles, 96 resources and 2,163
rows. Cumulatively it validates 940 custom anchors, 64,938 custom triangles,
289 selected resources and 16 zero-triangle fallbacks; the stone control brings
selected output to 64,948 triangles.

The isolated M4/M5 technical lifecycle produced these exact byte-identical
cold/warm reports and 79-row comparable manifests:

| Mode/phases | Report | Report SHA-256 | Manifest SHA-256 |
| --- | ---: | --- | --- |
| Initial and restored enabled cold/warm | 7,695,130 bytes | `6aaa925961ace51dfd2a7e074f9745d78e4aae28a334425f55498fa2bb5038fc` | `97a4f0915d6698e0962d6355f0835a2286a3ee7cae983ad1f3514c9ecb76b46f` |
| Combined-disabled cold/warm | 6,760,492 bytes | `acc180648556ba2a0fc4115172ba04aad4e0c266358379bf75f80f5353c62aa2` | `d457c23adc189c0e03718aad5c09649a7db63c1efb0174b439432df9f480ac56` |
| Crafting-disabled cold/warm | 5,956,875 bytes | `8b2b7b3b857c88f1c201399cffeeead588ac78e3ae3023d64ca4c3952a8b1593` | `dd86e95e7001ddb22e355f6910d7e7d3ded032d4eb2fcedc1a5ffd2deb1258cc` |
| Native-structural-disabled cold/warm | 5,697,202 bytes | `0742cc20f7fd7af776f1a342c52206e6ee6107c758146b9376358019b8a34a10` | `680ae50473f9829f396fc09f260e28221f283eda30a2b438688a81ca9788a668` |

Initial and restored enabled are exact matches. Full-JVM transitions and the
restart, initializer, worker, one-build, and settle gates passed. Packaging the
enabled comparable set produced the exact 20,821,895-byte candidate map
archive with SHA-256
`44422aa71c2f450951d8433e25e01de7a0b00dbd0d9c4fa4ff74ca98e649a2df`.
This closes technical validation only; owner visual acceptance remains
pending.

Enabled schema 9 validates 579 custom anchors, 26,580 custom triangles, 218
resolved resources, 17 zero-triangle fallbacks and the separate ten-triangle
stone control. Extension-disabled expects 547 custom anchors / 21,524 custom
triangles / 210 total selected resources. Glass-disabled expects 532 / 25,804
/ 199; crafting-disabled 494 / 22,274 / 203; quantum-disabled 552 / 25,392 /
216. M3-completion-disabled restores the exact original resource models at
all 78 new anchors and retains 501 custom anchors / 23,758 custom triangles /
207 total resources. The physical stock baseline contains the stone plus 38
stock-rendered M3f machines: 1,882 triangles and five resources; 40 M3f
anchors are stock-empty. The `--native-structural-disabled` mode has 589
custom anchors, 27,188 custom triangles, 218 resources, 17 fallbacks and
27,198 selected triangles. At the appended S1 positions it requires the exact
predecessor projection: ten rendered anchors, 350 empty anchors, 608 triangles
and 14 resources. All ten legacy upgrades must be empty. The physically add-
on-absent stock baseline selects 1,882 triangles across five resources and
requires 918 anchors empty, including all 360 appended S1 anchors and all ten
legacy-upgrade positions. These eight route-isolation/stock modes are mutually
exclusive.

PRBM retains no block ID, NBT, or renderer identity. Anchor ownership remains
a spatial inference. The manifest supplies the exact identity for each
selected position, while the case-11 opaque stone is present only as
unselected context geometry. S1's exact invariant signatures include geometry,
winding, UV, resolved material, normals, RGB and AO while excluding only
environment-derived blocklight and sunlight. Full observed attribute
signatures still retain both light channels. Every triangle must carry a flat
light pair in `0..15`, and the four smart/smart-dense channel resources remain
exact fullbright. The M3c contract checks exact corners, winding,
normals, materials, base and frame UVs, white RGB and AO 255. Base and frame
triangles on one face must share one valid light pair. Ordinary quartz-glass
blocklight and both variants' sunlight remain world-derived; every visible
vibrant-glass face must have blocklight 15 because the center state's emission
is a hard floor. Actual light stays in per-anchor/global attribute signatures
for cold/warm/restored equality but is excluded from topology and matched-
variant comparisons. Java tests are authoritative for the renderer's
center/outward maximum and cave behavior.

The M3d contract independently checks per-anchor faces, winding, normals,
materials, UVs, RGB, AO and light policy. Powered emissive overlays must be
fullbright; ordinary faces retain one world-derived light pair. Runtime
verification intentionally checks only stable formed/powered/facing/spin and
monitor `paintedColor` fields, never volatile proxy/core NBT.

The M3e contract checks exact cuboid topology, winding, normals, bounds-mapped
UVs, four-material closure, white RGB, the air-isolated neighbor-derived AO
result of 255 and consistent valid per-face world light. Java tests remain
authoritative for non-air neighbor AO and host light. PRBM cannot prove the saved `formed`/`waterlogged` state or source
renderer identity, so runtime verification binds those values at all 27
positions. Unformed, malformed, waterlogged, powered/QES and particle behavior
is deliberately limited to Java/synthetic evidence rather than unstable or
stock-visible physical anchors.

The M3f contract independently reconstructs persisted paint offsets and face
planes, ModelPart chest UV strips, exact crank and Inscriber JSON models,
neutral Inscriber stamps, all 24 machine transforms and every supported local
pylon axis/role texture+UV topology. It compares an orientation-preserving
triangle multiset at q16 position/UV precision, then exact encoded normals,
RGB and fixture AO. Manual primitives force AO 255; crank and Inscriber shell
retain host neighbor AO, which is also 255 in this air-isolated/non-occluding
fixture. Every transformed world face must share one valid host-light pair.

The M3b component-insensitivity comparison likewise excludes ordinary model
blocklight and sunlight while retaining geometry, materials, UVs, colors and
AO. Its separate per-anchor Drive contract requires every static LED to be
black, fullbright, AO one and geometrically exact. Historical schema-2 M1,
schema-3 M2, schema-4 M3a and schema-5 M3b reports remain evidence only for
their exact artifacts and galleries.

For the accepted M3a lifecycle, enabled cold, warm, and restored schema-4
reports were byte-identical at 939,040 bytes with SHA-256
`283f158df62486d92eb36f4a12b25a37381e6315304d1a1fdd76f66da9691e55`;
the restored 44-file manifest had SHA-256
`414cbdcc4a90d654b02e0a7e1b6d58fcb47f3b76ffa34bb4ac3f26d5ce258651`.
The physically add-on-absent stock cold/warm reports were byte-identical at
322,637 bytes with SHA-256
`ffca251826a5297d4c9b1dfba175e709cb5641b0bf924906d312cd50f57e873d`.

Packaging the restored M3a evidence twice produced the exact 20,320,750-byte
archive with SHA-256
`18cd08c9f0de6bc132bbf9f5cbf1d692d475864ca9aa818eed6166d892a94dce`.
It remains a retained local evidence archive rather than a release asset.

For the M3b technical lifecycle, enabled cold, warm and restored schema-5
reports were byte-identical at 1,335,214 bytes with SHA-256
`e5e466d9f207beba2032775b66acf091c5c3bf647b40bc811b62a714dae6dd8e`;
their 44-file manifests had SHA-256
`1b1db461fc8e76d4e988bd60c403af8601c51cf4d6fac0bfb4e748df5c4c60e9`.
They selected 342 custom anchors, 17,488 custom triangles, all 167 resources,
zero triangles in 16 fallbacks and ten triangles at the stone control.

The extension-disabled cold/warm reports were byte-identical at 1,006,824
bytes with SHA-256
`2947419d53cf7e2e2f8a4a3cacecc2515eb6aec2382f328e48fb596a98c9d754`;
their 44-file manifest SHA-256 was
`f82b43aa87d2f5b8e8e856f769bf138584b242cc53c99a73c5a6752be2de7fa1`.
All 36 M3b anchors were empty and the accepted M3a regression was exact. The
physically add-on-absent stock cold/warm reports were byte-identical at 365,850
bytes with SHA-256
`630c96be1406504267e27871e6113b0d98d23da522c9358c31f2f61efaff6f1a`;
only the stone control owned triangles, and the 44-file manifest SHA-256 was
`3ad852e739f4cacceee2fbc275a0ee3c6a231623a3a429fe2d0821915bb4b592`.

The fixture remained at zero failures and one build with six unchanged NBT
probes. Accepted capture pods had zero restarts and no players; one pre-runtime
operator init sequencing attempt ended before Minecraft started and
caused no add-on restart. Both render threads accumulated 420 and 418
scheduler jiffies. Packaging the restored 44 files twice produced the exact
20,351,418-byte archive with SHA-256
`c73844990847148d9cd3d315832085e49776e9253c5c8eca6f0b7659d73c4285`.

These results close the M3b technical lifecycle. The owner visually accepted
the exact JAR, gallery ZIP and map archive on 2026-08-07. M3c received its own
separate acceptance below.

For the M3c technical lifecycle, the exact 375,558-byte candidate JAR with
SHA-256
`4c1b557ae4c79c738005b74e2f0c89ca4fbe503dd6ef0ba614fae34d8e449d47`
and this exact 38,929-byte gallery ZIP passed the coordinated build and source
gates with 247 Java and 71 Python tests. The exact 1,517,248-byte schema-6
`cases.json` has SHA-256
`2d4fbba58ea2c4d3ed741e93a8dd9857523cac9cda021ffd3111e6ac51aec602`.

Enabled cold, warm and restored schema-6 reports were byte-identical at
1,582,336 bytes with SHA-256
`e81b8e6eed2047629a933a21e0e345c4880db2b12e264455fa84ef59b63d824f`;
their 44-file manifests were byte-identical with SHA-256
`3257a86e895956d7701f056ef46f3188fb4fca2f704b8c7e67641164666221f3`.
Extension-disabled cold/warm retained all 47 M3c anchors while emptying all 36
M3b anchors. Glass-disabled cold/warm emptied all 47 M3c anchors while retaining
the accepted M3b slice. Physically add-on-absent stock cold/warm selected only
the stone control. Every pair was independently byte-identical, and restored
enabled output reproduced the initial report and manifest without rebuilding.

The fixture remained at one build, zero failures and six unchanged critical
NBT probes through delayed verification, actual unload/reload, full JVM
restarts, all route modes and restoration. Every accepted capture pod had zero
restarts and both render threads accumulated CPU time. One operator Ctrl-C
while detaching the console caused a restart attempt during pre-lifecycle
setup; it preceded the accepted captures and was not an add-on restart.
Packaging the restored 44 files twice produced the exact 20,376,253-byte
archive with SHA-256
`3fb5fb174f23c0f2d8ce9f98e8c12feb8b12c444060e35b9d8e0036d8ec165e5`.
This closes the M3c technical lifecycle. On 2026-08-08 the owner also visually
accepted the exact 375,558-byte production JAR, this exact 38,929-byte gallery
ZIP and the exact 20,376,253-byte map archive. Their SHA-256 identities are
respectively
`4c1b557ae4c79c738005b74e2f0c89ca4fbe503dd6ef0ba614fae34d8e449d47`,
`0839009fe6a4f4785f864f33bc97fef28b8418f077d5d66a20efc3e8eeb4edab`
and
`3fb5fb174f23c0f2d8ce9f98e8c12feb8b12c444060e35b9d8e0036d8ec165e5`.
That bounded decision completes M3c. The candidate PVC aliases remain, and the
hash-exact accepted aliases are `ae2-addon-m3c-accepted.jar`,
`ae2-m3c-gallery-accepted.zip` and
`ae2-m3c-enabled-accepted-map-2026-08-08.tar.gz`; none is a publication store.

## M3e accepted technical/lifecycle and visual evidence

The exact 513,674-byte candidate JAR with SHA-256
`98ff55eaba609fc894b01e0c4d922b47f1871c324945f88f7a34864cf48b124f`
and 45,009-byte gallery ZIP with SHA-256
`498bac2f82b78451eb24da416ded1d625e5785cc5d3e5910b4c34bfecc05c390`
passed two byte-identical clean builds, 316 Java tests in 53 suites and 98
Python tests. The 3,123,572-byte schema-8 `cases.json` has SHA-256
`93963dd0bb60a276e1a17c6dd1f4eb916cd92bef4ef30a2e8bdc7a2bfa818b3e`.

On 2026-08-08 the fixture remained at one build, zero failures and two stable
checks through delayed verification, actual unload/reload, datapack reload,
complete JVM restarts, all six modes and final restoration. Every cold/warm
pair was byte-identical, and restored enabled reproduced initial enabled.
Report SHA-256 values were enabled/restored
`d306aff2a0f2eca2882a3d52426140b8caf98aeda76d0722dd14b59ea8a5e9e9`,
extension-disabled
`d871f95fd67e9e805829c37260e3dc43558cc4e388332d9771d388857a088f28`,
glass-disabled
`9a5c1103b55d6637d2b954c681dc15f333d944d2f59d983da2d1bf2645ad405c`,
crafting-disabled
`3b103035582b4db56dd81e08a3e20b7c80f839246f7d6a380a542b26994e5205`,
quantum-disabled
`13c4ca3676868418f9f6797e5b4b4816a38d7ed2c518e1cbb329063ff005e20b`
and physically absent stock
`0c3e4333782c244779a55201ab7a6362942e763c858bfdca2348b48204ce5e40`.
Their exact 44-file manifest identities are recorded in `docs/STAGING.md`.

All capture pods had zero restarts, and both render threads advanced by 587
and 245 scheduler jiffies. Packaging the restored 44 files twice produced the
same 20,424,799-byte, 44-entry archive with SHA-256
`9e145fffbe87205651ed7cc6b4cb706b7dcbe394ac26e7ce2eb1d6d55ea411a7`.
The candidate aliases `ae2-addon-m3e-candidate.jar`,
`ae2-m3e-gallery-candidate.zip` and
`ae2-m3e-enabled-candidate-map-2026-08-08.tar.gz` remain byte-identical to the
accepted aliases `ae2-addon-m3e-accepted.jar`,
`ae2-m3e-gallery-accepted.zip` and
`ae2-m3e-enabled-accepted-map-2026-08-08.tar.gz`. On 2026-08-08 the owner
visually accepted the exact JAR, gallery and archive. M3e was the latest
accepted slice at that checkpoint and M3d remained its previous rollback.

## M3f accepted technical/lifecycle and visual evidence

The exact 623,591-byte production JAR with SHA-256
`ca67c0fc433e43f8e0801ed8d2cccfe47aae317fbc329c099bc8cd741ec3b42b`
and exact 49,679-byte schema-9 gallery ZIP with SHA-256
`21ceec072cc3263a41bdb81874e897d48d5a1ce5e1c7d3ac3c0de3063818ee6c`
passed two byte-identical builds, 365 Java tests in 66 suites and 116 Python
tests. The corresponding sources JAR is 276,986 bytes with SHA-256
`2a3bb3713ff56731992d405a58fc6a137dcfc8fff43467de7196ad33c444795c`;
the 1,637-byte POM is
`967132ef80201099cfb1a798f03ff1ac37e0ac84a551694d7276ac20c7ccc136`;
and the 2,859-byte Gradle module metadata is
`106df734036c9f72f0463a03e2a282d430653edab5e761b165b51fe99f937d7a`.
The 9,405-byte completion profile has SHA-256
`281a335d3024ebbb97c6268e768826c467d6f7ea660989fd3dae204c6c03abf3`.
Its 3,738-byte required-resource manifest has 33 rows and SHA-256
`3faf7f29e2878f5525541bad855cbc66b6d45786dc8fc6ee29a6fbbf4878cca1`;
those rows bind an exact 33-resource, 22,491-byte upstream closure.

The prior M3f candidate was withdrawn after owner visual review exposed the
missing members of the L/T pylon arrangements. It was never accepted. The
corrected artifact above uses a 256-pylon-capped native-axis scan and renders
every member of a fully observed invalid component with AE2's unformed
BASE-plus-DIM appearance.

Enabled cold, warm and final restored schema-9 reports are byte-identical at
3,783,797 bytes with SHA-256
`7022a33448dab364cb825a8d67359795560b6a8793b64544ccb0b4c1fda7484e`;
their exact 44-path manifests are byte-identical at 3,893 bytes with SHA-256
`259af4eea91a32acc07d1572e8f3f42e6276b46999496d0380ec009c10970fd8`.
The M3-completion-disabled cold/warm report is 3,646,167 bytes with SHA-256
`c9391aacb0fa7e268b67bc4723ffa8958593acbf06e529542c63fc7fa59ba707`;
its manifest SHA-256 is
`34bad6b250e18217485d68dfb989cfbc6d9b53f480be71c534f91675dc3c6a3e`.
Physically add-on-absent stock cold/warm reports are byte-identical at 614,214
bytes with SHA-256
`30439e78593dacc43a0d3822f039ebbbd058d83be64023650471dab07179c4bc`;
their 3,893-byte manifests are byte-identical with SHA-256
`b66f1e00f658d8fbd6ed842aae614b5db7bc94580215b2ad22a2a769e4d73efa`.
That physical stock view contains 38 rendered M3f machines and 40 stock-empty
M3f anchors. Every 44-path manifest was independently rehashed and excludes
the two live files and six transient renderer-state files.

The fixture ended at `#m3f_builds=1`, `#failures=0` and `#stable=2` through
delayed verification, unload/reload, complete JVM restarts, route isolation,
physical stock and final restoration. The restored capture pod and all four
init containers had zero restarts; four observed render workers advanced by
22, 16, 22 and 14 scheduler jiffies. Packaging the restored manifest twice
produced the same 20,450,880-byte, exact 44-entry archive with SHA-256
`e66abf203481c5df0fa0fc0062c414876f9ef6428cd637de0795f821496c51a9`.
The candidate aliases are `ae2-addon-m3f-candidate.jar`,
`ae2-m3f-gallery-candidate.zip` and
`ae2-m3f-enabled-candidate-map-2026-08-09.tar.gz`. The hash-exact accepted
aliases are `ae2-addon-m3f-accepted.jar`, `ae2-m3f-gallery-accepted.zip` and
`ae2-m3f-enabled-accepted-map-2026-08-09.tar.gz`. The M3e candidate and
accepted aliases remain hash-exact and unchanged.

On 2026-08-09 the owner visually accepted the exact corrected JAR, gallery and
map archive above. That bounded decision completed M3 and made M3f the latest
accepted slice at that checkpoint; M3e was its previous accepted rollback
checkpoint. Machine
contents, held items, fluids, live/activity-specific state and accurate Drive
LEDs are accepted non-goals. S1 implements the native cable-bus structural
step as the accepted local checkpoint recorded above. Its exact All the Mons
1.2.0 enabled/native-structural-disabled/physical-stock/restored lifecycle
passed on 2026-08-11. Initial and restored enabled cold/warm captures share
report SHA-256
`14aa3b46386bead1f656f9796305c0000e835e5948ae06367d947a3afe837723`
and 46-file manifest SHA-256
`e1e592faabd263e1b9bacce14d56577f330d1b5cbd80336f2bd1563d3f1b2a78`.
The fixture stayed at `#m3f_builds=1`, `#s1_builds=1`, `#stable=2` and
`#failures=0`; every pod had zero restarts, all initializers exited zero and
both render workers advanced. The owner completed the bounded BlueMap visual
review for the exact S1 artifact set on 2026-08-11. S1 remains the latest exact
human-accepted local checkpoint. M4/M5 is now the latest owner-accepted
checkpoint under the schema-11 evidence above. The owner separately authorized
publication on 2026-08-12; remote creation, tags, Maven, and root integration
still require the exact release sequence.
