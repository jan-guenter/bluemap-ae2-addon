# Notices

Copyright (C) 2026 Jan Günter

This project is an independently packaged BlueMap add-on implementing
compatibility with Applied Energistics 2.

Applied Energistics 2 is copyright its authors and contributors. Its
implementation code is distributed under LGPL-3.0-or-later; its API, assets
and text use separately documented licenses. The milestone-bounded persistence
and rendering behavior was studied at exact tag `neoforge/v19.2.17`, commit
`79ee2c704ad62941a426c26b1cb1f76ef5b2ee5a`.

The official `appliedenergistics2-19.2.17-sources.jar` used to correlate the
nine M0-critical files, ten M3e quantum-bridge files, thirty M3f
completion-route files, and the 81-class/79-source-marker-file S1 native
structural audit has SHA-256
`d2f451203cb61c2d21fae52c683083d2f72441ca7d26725f4df5934290492e6a`.
That source archive is not redistributed.

ExtendedAE is copyright GlodBlock and its contributors. Historical accepted
M3b/M3d evidence studied exact `1.21-2.2.33-neoforge` at tag commit
`90005ee29839fb9fa83bbe6544919c722f8b0dc6`; the current All the Mons 1.2.0 S1
retarget studies exact `1.21-2.2.35-neoforge` at tag commit
`3776bc854458301bbcc9a44a8238d70a0e3dc00d`. Its mod metadata declares
`LGPL-3.0`, and its source tags include the GNU Lesser General Public License
version 3 text. Upstream credits Sea_Kerman for models and Ridanisaurus for
the new textures. No ExtendedAE source, class, model, texture, binary or JAR
is redistributed by this project.

Exact AppliedFlux `1.21-2.1.5-neoforge`, ME Requester `1.21.1-1.4.3`,
Expanded AE `2.1.1`, MEGA Cells `4.11.0`, Advanced AE `1.6.12-1.21.1`, Athena
`4.0.6`, and ExtendedAE `1.21-2.2.35-neoforge` runtime artifacts are the All
the Mons 1.2.0 M4/M5 evidence inputs. AppliedFlux, ME Requester, Expanded AE,
MEGA Cells, Advanced AE, and ExtendedAE declare LGPL-3.0-family terms. Athena
declares MIT and credits ThatGravyBoat as author. Glassential Renewed `3.4.5`
remains exact S1 facade-material evidence and also declares MIT. The existing
LGPLv3/GPLv3 and MIT license texts under `LICENSES/` remain unchanged. Because
these third-party artifacts are evidence and resource providers only and none
of their content is redistributed, no additional upstream license file is
bundled.

Published `0.1.0-alpha.2` additionally studies exact Applied
Mekanistics `1.6.3` at tag commit
`137f24bb9a46775ddd5a620055270b5e8a540f5a` and exact Mekanism mod version
`10.7.19` from `Mekanism-1.21.1-10.7.19.85.jar`, correlated to exact source
tag commit `a00109e4856fd38b9c5b3dd7f22ce4a59cd65a80`. Applied Mekanistics declares
code `LGPL-3.0-or-later` and assets `CC BY-NC-SA 3.0`. Mekanism declares MIT
and is copyright 2017-2025 Aidan C. Brady.

Only independently written compatibility code and bounded identifiers,
sizes, cryptographic digests, and semantic signatures are packaged. No
Applied Mekanistics or Mekanism source, class, model, texture, binary, or JAR
is redistributed. Their runtime resources remain operator installed. Its
exact runtime review was owner accepted and the release was published as
immutable prerelease `v0.1.0-alpha.2` on 2026-08-20.

Version `0.1.0-alpha.3` adds an independently written data-only extension API.
Its Ars Energistique example packages only identifiers and model paths supplied
for registration. No Ars Energistique or Ars Nouveau source, class, model,
texture, binary, or JAR is redistributed; the soft-dependent add-on resolves
and validates its own operator-installed resources before route activation.

The M4/M5 implementation packages independently written adapter, catalog, and
geometry code plus identifiers, sizes, and cryptographic digests. No source,
class, model, texture, animation metadata, binary, JAR, client capture, or
precomputed mesh from any project above is redistributed. Every upstream
visual resource is loaded from the operator-installed exact JAR. Exact
artifact, resource-manifest, and catalog identities are recorded in
`provenance/upstreams.json` and `docs/PROVENANCE.md`.

BlueMap is copyright Blue (Lukas Rieger) and contributors and is distributed
under the MIT License. The current runtime host is the native-5.23 feature
backport at commit `7e07f4e74ec1e92a6ead9aa1e66054af3e133aac`; its
6,545,661-byte NeoForge JAR has SHA-256
`86a0323d24f472e425dda4d4e6bba2d7d8ce8315ad009930a67131128c715e62`.
The accepted M0-M3f runtime record instead remains bound to historical
backport commit `fe79cf5b9f4d8ca28f4e41c2aeb9ef792e336a8d`.
This project adapts exact-ABI integration, ambient-
occlusion, map-color and original-resource-fallback patterns into its local
LGPL adapter but does not bundle BlueMap source or binaries. The complete MIT
copyright and permission notice is retained in `LICENSES/MIT.txt` and in the
production and sources JARs.

The add-on compiles and packages the four MIT-licensed shared Adapter API
classes from commit `e81f08bc4bfbf02d810ec8949a019130e2e61634`. The complete
Adapter API MIT license is packaged as `META-INF/LICENSE-bluemap-addon-adapter-api`.

## Modification notice

On 2026-08-03, this project adapted the exact AE2 19.2.17 cable-core and glass-
connection dimensions, UV rules and straight-line selection into a bounded,
independent neutral geometry implementation. It also adapted the persisted
center-part field names into a reduced interoperability DTO.

On 2026-08-04, the local M1 source adaptation expanded that neutral
implementation to the exact 85 cable IDs, the five cable families, their
color/family compatibility and geometry rules, and the static zero-channel
smart-cable overlays. The complete exact source-blob identities are recorded
in `provenance/upstreams.json`. No AE2 class, texture, model, binary or source
archive is bundled.

The same date records the BlueMap 5.22 adapter adaptation described above.
The resulting local files are modified and distributed under this project's
LGPL terms while preserving BlueMap's MIT notice.

On 2026-08-07, the M3a adaptation added the exact native AE2 Drive catalog,
ten-slot identity, static offline-unknown LED policy and atomic fallback for
unknown extension cells. The exact 259,005-byte M3a artifact with SHA-256
`55a11805373aebfde821e5009723ec7d672fb290127dbc60131ffa344c99518a`
completed its bounded technical and human visual acceptance on that date.

On 2026-08-07, the local M3b source adaptation added a local ExtendedAE Ex
Drive implementation candidate plus its exact profile and resource contract:
the twenty-slot two-sided layout, three built-in cell-model mappings, disjoint
15-resource digest contract and dependency on the already pinned AE2 M3a Drive
resource partition. The exact M3b artifact subsequently completed its bounded
technical lifecycle and human visual acceptance on 2026-08-07. The
implementation records identifiers, sizes and digests; the operator-installed
ExtendedAE and AE2 resources remain outside the add-on.

On 2026-08-07, the M3c audit and local source adaptation added a disjoint exact
renderer, profile, schema-6 synthetic evidence lane and 22-resource contract
for `ae2:quartz_glass` and `ae2:quartz_vibrant_glass`. It records the
six-neighbor connected-glass topology, position-seeded texture policy and
exact source/class identities. BlueMap does not expose Minecraft's reciprocal
`getAppearance` query, so known non-native appearance proxies are treated as
disconnected; missing or malformed native neighbor data uses atomic
original-resource fallback. BlueMap 5.22 PRBM also has no representation of
AE2's `BakedQuad shade=false` flag, so this adaptation makes no
pixel-identical directional-lighting claim. Client particles and item
rendering are outside the world-block renderer. No AE2 class, model, texture,
capture or precomputed mesh is incorporated. On 2026-08-08, the exact
375,558-byte artifact with SHA-256
`4c1b557ae4c79c738005b74e2f0c89ca4fbe503dd6ef0ba614fae34d8e449d47`,
the exact 38,929-byte schema-6 gallery with SHA-256
`0839009fe6a4f4785f864f33bc97fef28b8418f077d5d66a20efc3e8eeb4edab`,
and the exact 20,376,253-byte map archive with SHA-256
`3fb5fb174f23c0f2d8ce9f98e8c12feb8b12c444060e35b9d8e0036d8ec165e5`
completed the bounded M3c technical lifecycle and human visual acceptance.

On 2026-08-08, the M3d audit and local source adaptation added an independent
exact profile, synthetic route and 30-resource contract for the eight native
AE2 formed-crafting blocks. It records the formed and powered state, six-axis
crafting-cluster connections, monitor orientation and painted color, omission
of the client-stream-only displayed item, and AE2's force-solid/occluding
block policy. Exact pack bytecode admits eight MEGA Cells `4.11.0` and 21
Expanded AE `2.1.1` block IDs only as compatible-but-unsupported connectors;
Advanced AE `1.6.11-1.21.1` and ExtendedAE
`1.21-2.2.33-neoforge` are explicitly outside that native-subclass boundary.
The implementation records only identifiers, sizes and digests; all models,
textures and evidence artifacts remain operator installed. The exact M3d
artifact, schema-7 gallery and map archive completed their bounded technical
lifecycle and human visual acceptance on 2026-08-08.

The M3e audit and local source adaptation add an independent exact profile,
renderer, and 13-resource contract for
`ae2:quantum_link` and `ae2:quantum_ring`. Its authoritative saved boundary is
the `formed` and `waterlogged` block state. Constructed role, corner and power
are client-stream-only and are deliberately not decoded, so the projection is
static-off-unknown and excludes powered overlays and particles. The exact
runtime JAR and exact 3,814,167-byte official sources JAR with SHA-256
`d2f451203cb61c2d21fae52c683083d2f72441ca7d26725f4df5934290492e6a`
are verified external evidence and are not redistributed. Six source texture
identifiers are audited; only four non-powered texture identifiers are emitted.
No extension connector compatibility is claimed. On 2026-08-08, the exact
513,674-byte artifact with SHA-256
`98ff55eaba609fc894b01e0c4d922b47f1871c324945f88f7a34864cf48b124f`,
the exact 45,009-byte schema-8 gallery with SHA-256
`498bac2f82b78451eb24da416ded1d625e5785cc5d3e5910b4c34bfecc05c390`,
and the exact 20,424,799-byte map archive with SHA-256
`9e145fffbe87205651ed7cc6b4cb706b7dcbe394ac26e7ce2eb1d6d55ea411a7`
completed the bounded M3e technical lifecycle and human visual acceptance.

The M3f audit and local source adaptation add one fail-closed completion route
for `ae2:paint`, both native Sky Stone chest IDs, `ae2:crank`,
`ae2:inscriber`, and `ae2:spatial_pylon`. The route retains only the bounded
persisted paint-splotch data and block-state orientation needed for static
world rendering. Chests are closed, the crank and inscriber are neutral, and
items, inventory contents, fluids, lid/machine animation, and particles are
excluded. Spatial-pylon start, middle, and end roles use an uncached native
axis-line scan capped at 256 pylons. A bounded locally invalid bend or branch
component renders AE2's unformed NONE role with BASE plus DIM materials;
missing or malformed observations and capped scans fall back atomically.
Global cluster validity beyond that bounded local component, online state, and
power are not claimed, and the emitted materials are static-offline-unknown.
The exact 33-resource closure records 17 source textures, 15 emitted static
textures, and two fallback-only textures. All resources remain operator
installed. The exact corrected 623,591-byte M3f artifact, 49,679-byte schema-9
gallery, and 20,450,880-byte map archive completed the bounded technical
lifecycle and human visual acceptance on 2026-08-09.

The S1 native structural audit adds an independent exact schema-10 profile for
all 29 non-center native face parts, the nine persisted reporting-part spins,
the sole dense-capable `ae2:cable_anchor`, 30 native attached-device endpoints,
all 64 facade masks, and per-instance valid static facade block-state
materials. The exact facade contract includes all 24 explicit whitelist
families, their pinned neutral/default persisted states and
`BlockState.isSolidRender` values, plus a bounded optional-tag/ordinary static
full-cube-witness material lane. One unrotated full-cube witness is required;
bounded extra static elements and multipart source quads remain admitted under
the UV, tint, weighted-variant, and semantic-resource gates. Otherwise-valid
complex material models fall back atomically. The eleven AE2-native entries
are a resource/normalization subset, not the complete facade support set. All
24 explicit whitelist families require exact complete persisted BlockState
keys and source-declared value domains; valid vanilla states are preserved,
native dynamic states are normalized as declared, and missing, extra, or
out-of-domain properties fall back atomically. All
30 native endpoints require exact persisted BlockState keys/value domains and
serialized block-entity IDs, including endpoints whose side rule is ALL.
`isSolidRender` and same-state `skipRendering` are family-invariant across the
complete 554-state explicit-whitelist Cartesian closure; the neutral/default
row booleans therefore apply to every valid state in that family.
Facade materials may contain no tint or one shared nonnegative source tint
index, with untinted layers alongside it; mixed nonnegative tint indexes fall
back atomically because the host exposes one block-color projection.
UVs are reinterpolated only through AE2's nominal-face two-dimensional
`QuadReInterpolator` projection; projected quads without a complete exact
`InterpHelper` grid fall back atomically instead of using a three-dimensional
planar approximation.
Cardinal block-state variant geometry/vectors and uvlock coordinates use exact
signed-permutation quarter-turns so a host floating-point matrix cannot perturb
exact `InterpHelper` grid points.
Exact stone alone retains the accepted M2 geometry-and-material position
projection without claiming pixel-identical client-randomized UV orientation;
other weighted sets must collapse across every alternative to one static
geometry/material/UV descriptor or fall back atomically.
The exact 24-entry same-state `BlockState.skipRendering` table is true only for
the two AE2 quartz-glass families and `minecraft:honey_block`; quartz and
vibrant quartz also skip one another when their render shapes match. Exact
ordinary gallery controls pin glass true and oak log/leaves false, while other
ordinary/tag materials retain the bounded BlueMap same-state culling host
projection. Corner-kick comparison uses `0.00001` block units in AE2 and the
PRBM analyzer, and `0.00016` sixteenth units in runtime geometry. Minecraft
`AABB` endpoint normalization is retained for reversed facade-strip endpoints.
Plane connection bits retain AE2's front-view logical
`up=8/right=4/down=2/left=1` contract. `PlaneBakedModel` visual-local bounds
remain separate and are transformed into installed-world space by
`QuadRotator`; facade collision uses the exact face-aware installed-face-local
axes from `PlaneConnectionHelper` and `BusCollisionHelper`. Bounds are not
reused across those coordinate spaces.
Facade AO samples the source face direction rotated by the block-state variant;
element rotation changes vertices, not that AO direction. Element emission
affects triangle blocklight but never brightens structural map color, which
uses BlueMap's original center/outward world-light samples.
Malformed native observations and known extension UNKNOWN observations use
distinct atomic fallback branches; unrelated non-native block entities remain
disconnected. A known extension block is admitted compatible only with its
exact catalogued serialized block-entity ID; a missing or wrong ID is malformed
UNKNOWN, while an unrelated block carrying that ID remains disconnected. The
exact 41-root closure resolves to 43 JSON models and 56 PNG
textures across 99 operator-installed resources and 51,306 bytes. Parsed live
model and decoded texture-plus-animation metadata signatures are locked, as
is the separate nineteen-texture quartz-facade dependency. BlueMap 5.22 has no
per-quad shade channel, so the source shade bit is evidence-locked but no
pixel-exact directional-shade claim is made. Only identifiers, sizes, and
digests are packaged; no AE2 model, texture, source, class, capture, or mesh is
redistributed. Machine contents, held items, fluids, live activity, and
accurate Drive LED states remain excluded. This S1 profile/source/resource
candidate was revalidated as a reproducible local build for All the Mons
`1.2.0` at pack commit `c7bb230f21d14d26859d0b92548f089b3a493ad9` and
NeoForge `21.1.248`. Its exact current Advanced AE, ExtendedAE, Glassential and
BlueMap inputs are recorded above. Its isolated lifecycle and owner visual
review completed on 2026-08-11 for the exact accepted S1 artifact set; the
historical All the Mons 1.1.1 / NeoForge 21.1.234 M0-M3f acceptance is not
transferred.

The M4/M5 adaptation adds eight independently fail-closed routes. AppliedFlux
adds its static Flux Accessor part and exact Drive-cell catalog. ME Requester
adds exact saved requester transforms and an offline requester-terminal.
Expanded AE adds the I/O Port, two parts, and 21 formed-crafting blocks. MEGA
Cells adds eight formed-crafting blocks, three generic parts, Cell Dock, and a
67-item/37-model static cell catalog. Advanced AE adds eight quantum-computer
roles under an unpowered/light-zero projection; its separate quantum-alloy CTM
uses exact Athena 4.0.6 five-texture selection with generated static frame-zero
textures. ExtendedAE adds six Assembler Matrix roles and two static cable-bus
planes while retaining its exact current Extended Drive behavior.

The exact M45 verifier covers AE2 plus all seven external M4/M5 inputs, eleven
resource manifests with 375 rows, and the 67-row MEGA cell catalog. Live
power, channels, activity, requested jobs, displayed stacks, inventories,
fluids, animation, and accurate LEDs are deliberately not adapted. The Java
runtime audit, bounded implementation tests, final reproducible artifacts,
schema-11 isolated lifecycle, deterministic live-map evidence, and human
visual acceptance completed on 2026-08-12. The exact result was published as
immutable prerelease `v0.1.0-alpha.1`. Applied Mekanistics alpha.2 has separate
accepted runtime evidence and was published as immutable prerelease
`v0.1.0-alpha.2`. Neither result transfers to the bounded alpha.3 Ars
Energistique registration-hook staging.

AE2 textures and models remain under their upstream CC BY-NC-SA 3.0 terms and
are resolved only from the operator-installed resource pack. They are not
incorporated into the add-on's LGPL-covered output.

The complete source-use record is in `provenance/upstreams.json` and
`docs/PROVENANCE.md`.
